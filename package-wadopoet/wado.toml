[package]
name = "wadopoet"
description = "Builders for generated Wado source, and the vocabulary a generated name must avoid"
repository-directory = "package-wadopoet"
lib = "src/lib.wado"
# version / repository / namespace / license / authors inherited from [workspace.package]
