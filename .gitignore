target/
build/

*.wasm

# mise
.mise.local.toml

# vendor (cloned for reference, not submodules)
vendor/zlib/

# test artifacts
wado-compiler/tests/fixtures/testdata/test_output.txt

# Gale Stage B′ oracle-pending manifest: transient hand-off from
# `extract_antlr4_descriptors.wado` to
# `scripts/extract-antlr4-descriptors.sh`. The shell wrapper drains
# this dir into `tests/antlr4-compat/stage_b_oracle/`, which IS
# checked in (that's where the actual expected trees live).
package-gale/tests/antlr4-compat/oracle-pending/

# plan files
plan.md
PLAN.md
profile.json
*.jitdump
jit-*.dump

# npm
node_modules/

# dotenv
.env
.env.*

# claude code
.claude/scheduled_tasks.lock
.claude/worktrees/

# Python bytecode cache (e.g. from profiler/analysis scripts)
__pycache__/
*.pyc
