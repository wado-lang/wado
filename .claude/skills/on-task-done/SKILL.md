---
name: on-task-done
description: Completing the task in a branch
---

# Overview

When you finish a task, you'd better reviewing docs if necessary:

- docs/spec.md
- docs/cheatsheet.md
- docs/compiler.md
- docs/optimizer.md

Then, run `time mise run on-task-done`, which includes:

```sh
cargo build
mise run clippy-fix
mise run update-golden-fixtures
mise run update-golden-format-fixtures
mise run update-gale-golden
mise run doc-stdlib
mise run format
mise run test
mise run test-wado
```

`on-task-done` will take 20+ minutes.

## Notes

- Run it without `| tail` in order not to lose the results.
- The changes generated by the command are necessary. Commit them.
- The process might be killed by the supervisor if you just wait for its completion. Watch the progress periodically in order to tell the supervisor you are still working in progress.
