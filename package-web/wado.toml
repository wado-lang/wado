[package]
name = "web"
description = "Web platform bindings for Wado, generated from WebIDL"
repository-directory = "package-web"
lib = "src/dom.wado"
# version / repository / namespace / license / authors inherited from [workspace.package]
