# WasmとAgentic Coding革命の交差点: The Wado Programming Language

このプログラミング言語は、2026年1月3日にinitial commitが行われました。それから一ヶ月も経っていない今、静的型付け、ジェネリクス、クロージャ、モジュールシステム、そして実用的なパフォーマンスを備えた処理系が動いています。

開発者は私一人です。スタートアップで働きながら、2児を育てる傍らでの開発です。そして何より驚くべきことに——**コードの100%以上はAI agentsが書きました**。（私が時々コメントやdead codeを消すので、正確には105%くらいがagentic codingかもしれません）

これは誇張でも冗談でもありません。2026年、個人が片手間でプログラミング言語を設計し、処理系を完成させられる時代が、本当に来たのです。

## Agentic Coding革命が突きつけた問い

半年前、私は["Agentic Coding革命は「来て」いる"](https://gfx.hatenablog.com/entry/2025/07/06/182751)というブログを書きました。AI agentsによるコーディングが、不可逆的な変化をもたらしていることを記録したものです。

そして今、Wadoの開発を通じて実感しています。この革命は、プログラミング言語に求められる性質を根本から変えつつあります。

### パフォーマンスの重要性が、まさかの再浮上

agentic codingで同じ開発時間を使うなら、実行時性能の差は決定的な選択要因になります。もしある言語が別の言語より10倍以上遅いとしたら、遅い方を選ぶ理由がありません。

Wadoのベンチマーク結果を見てください（x86_64アーキテクチャ、wasmtime 40.0.2）:

**整数演算ベンチマーク（素数カウント、10,000,000まで）**
- C (gcc -O3): 3,450ms
- **Wado (wasmtime)**: 3,717ms（Cの1.08倍）
- JavaScript (Node.js): 4,319ms（Cの1.25倍）
- Ruby: 65,847ms（Cの19.1倍）
- Python: 81,775ms（Cの23.7倍）

**浮動小数点演算ベンチマーク（Mandelbrot、1024x768）**
- C (gcc -O3): 150ms
- JavaScript (Node.js): 179ms（Cの1.19倍）
- **Wado (wasmtime)**: 185ms（Cの1.23倍）
- Python: 4,351ms（Cの29.0倍）
- Ruby: 5,994ms（Cの40.0倍）

ペライチファイルを`wado run hello.wado`で実行できるスクリプト言語として、この性能は破格です。Pythonより20〜30倍速く、ネイティブコードに迫る速度で動作します。

### 明示性と予測可能性こそが生命線

「ほかのファイルを参照しなければならない」「コード上に現れない暗黙のコスト」——これらはAI agentsにとって扱いにくい性質です。強い型付け、明示的なエラーハンドリング、大域脱出をする例外がないこと。これらは、agentic codingの質を劇的に安定させます。

coding agentsが`try-catch`を書きまくって辟易した経験、ありますよね？TypeScriptの`any`のような脱出ハッチが「そもそも用意されていない」ことは、品質の安定化に直結します。

Wadoには`panic()`と`unreachable()`はありますが、これらはWadoレベルではcatch不可能です。エラーハンドリングは全て`Result<T, E>`で行います。大域脱出例外は、ありません。

### 学習可能性の予想外の変化

「新しい言語はcoding agentsにとって使いづらいのでは？」——そう思っていました。しかし、cheatsheet（やagent skills）があれば、AI agentsは驚くほど効率的に使いこなします。むしろ、独自すぎる構文やセマンティクスを持たない方がいいことが分かってきました。

Wadoは、既存の言語（Rust、TypeScript、Go）の良い部分を組み合わせつつ、意図的にマクロや複雑な機能を排除しています。構文はシンプルで予測しやすく、セマンティクスは一貫しています。

## なぜWadoを作ったのか

### 直接のきっかけ：AssemblyScriptとの出会い

あるOSSの一部をWasmで実装しようとして、当然AssemblyScriptに行き着きました。ASはWasm実装の言語としてかなり良いです。しかし、使っているうちに気づいたのです。

Wasm初期の設計を引きずっていること。TypeScript/JavaScriptのレガシーを引きずっていること。構文がWasmと親和性が高くないこと。

そして思いました。「Wasmが透けて見える」zero abstraction言語が、欲しい。

### タイミングの幸運：2026年という時期

まさに今が、絶好のタイミングでした。

Wasm Component ModelとWASI P3（Preview 3）の仕様が固まってきたことで、これらを前提とすることで実装や意思決定が大幅に簡略化できました。wasmtime v40を使うとネイティブの80〜90%程度の速度で動作します。Wasm GCを活用することで、ライフタイム管理を不要にしつつ、wasmサイズも削減できます。

5年前だったら、この判断はできませんでした。1年後だったら、もう遅すぎたかもしれません。2026年1月という、まさにこのタイミングだったからこそです。

### Wasm GC: 性能とシンプルさのトレードオフ

Wasm GCの採用は、pros/consがあります。GCのオーバーヘッドは確実にあります。しかし、「Wasmが透けて見える軽量な言語」という観点からは、Wasm GCの活用は重要な選択でした。

ライフタイム管理がないことで、言語仕様がシンプルになります。wasmサイズが削減できます。そして何より、agentic codingとの相性が良いのです。

実際のwasmバイナリサイズを見てください：

**Hello World（最小プログラム）**
- **Wado**: 1,082 bytes
- AssemblyScript: 6,913 bytes（Wadoの6.4倍）
- Rust: 42,587 bytes（Wadoの39.4倍）

**Pi近似計算（浮動小数点演算）**
- AssemblyScript: 11,372 bytes
- **Wado**: 11,789 bytes（ASとほぼ同等）
- Rust: 62,952 bytes（Wadoの5.3倍）

1KBでHello Worldが動く。これは、Wasm GCによるランタイムサイズ削減の成果です。

### 設計と開発の、ありえないスピード

構文についてのディスカッションをcoding agentsとすることで、一人で思索にふけるよりずっと効率的に設計ができました。

構文の試行錯誤は、驚くほど簡単です。「こういう構文を実装したい」と思ってから、1時間もするとできあがります。構文の変更も、同様に簡単です。（構文変更はセマンティクスの変更や新機能の追加よりずっと簡単なのです！）

何もかもが「ありえないスピードで開発が進む」。これが、agentic coding時代の現実です。

## プログラミング言語への思い入れ

少し個人的な話をさせてください。

私はもともと、Perlが母国語と言っていいくらい精通しています。Perlの"better shell+sed+awk, easier C"という設計背景は今でも好きですし、"Easy things should be easy and hard things should be possible"というLarry Wallの設計哲学は私の座右の銘です。プログラマの三大美徳も、私のソフトウェアエンジニアとしての価値観に強い影響を与えています。（まあ、TMTOWTDIは今の時代ちょっと違うかなとは思いますが）

一方で、CやC++やJavaを使う傍らで静的型付け言語に傾倒してゆき、今はRustやTypeScriptがお気に入りの言語です。しかし、それぞれに欠点があります。

**Rust**——素晴らしい言語です！しかし、ライフタイムとそれに伴う世界で一番複雑な型システム、そしてマクロ。結果として、コンパイルの遅さは開発効率に無視できない悪影響を与えています。`cfg`/`features`による複雑なコンパイル条件分岐も、メンテナンスを難しくしています。

**TypeScript**——素晴らしい言語です！『JavaScriptプログラマーのためのTypeScript厳選ガイド』という単著も書いている程度には好きで、よく知っている言語です。しかし、JavaScriptのレガシーを引きずっていて意味不明な仕様がたくさんありますし、使うべきでない機能が多すぎてlint configのメンテに無限に時間が吸われます。これまた複雑すぎる型システムとコンパイル時間の増大も問題です。（まあ`tsgo`で大幅に改善はしますが）

Wadoは、これらの言語の良い部分を学びつつ、agentic coding時代に最適化された新しい選択肢を目指しています。

## Wadoの特徴

開発を通じて見えてきた、Wadoの特徴をまとめます。

**Wasm/WASI P3専用**
- ネイティブに近い性能（上記ベンチマーク参照）
- UB（未定義動作）やunsafeな動作なし
- Component Model対応、WASI P3の豊富な機能を活用

**Wasm GC活用**
- ライフタイム管理不要
- 参照のGCによる自動管理
- wasmサイズの削減

**強い静的型付け**
- ジェネリクス対応
- 型推論
- 脱出ハッチなし（`any`のような型はありません）

**明示的エラーハンドリング**
- 大域脱出例外なし
- `Result<T, E>`によるエラー処理
- `panic()`と`unreachable()`はあるが、Wadoレベルではcatch不可能

**シンプルで予測しやすい構文**
- マクロなし（意図的に入れない判断）
- 既存言語（Rust、TypeScript、Go）からの良い部分の組み合わせ
- 一貫したセマンティクス

**coding agent friendly**
- 充実したcheatsheet
- `wado dump`コマンドによる内部状態の可視化
- `--ast`、`--tir`、`--lower`、`--optimize`など、各フェーズの出力確認可能
- `--unparse`による中間表現のソースコード化

## Agentic Coding時代のツールチェイン

特筆すべきは、最初からcoding agent friendlyなツールチェインを意識して作っていることです。

`wado dump`コマンドは、コンパイラの内部状態を詳細に確認できます：

```bash
wado dump file.wado                  # 全フェーズを表示
wado dump --ast file.wado            # AST構造を表示
wado dump --ast --unparse file.wado  # ASTをWadoソースコードとして表示
wado dump --tir file.wado            # 型付きIRを表示
wado dump --tir --unparse file.wado  # TIRを疑似ソースコードとして表示
wado dump --optimize file.wado       # 最適化ヒントを表示
wado dump --optimize --unparse -O2 file.wado  # 最適化後のTIRをソースコード化
```

coding agentsは、このツールを驚くほど効果的に使っています。問題を診断し、修正し、検証する。この一連のフローが、人間よりも速く回ることもあります。

## これは「事件の記録」です

これは単なるプログラミング言語の紹介ではありません。agentic coding革命という「事件」の記録です。

2026年1月から数週間で、AI agentsと協働しながら、個人が新しいプログラミング言語を設計し実装できました。5年前なら考えられなかったことです。いや、1年前でも難しかったでしょう。

Wadoそのものは、まだ発展途上です。`enum`のcodegen、`match`式、`trait`、effect handlers、closureのcapture——実装すべき機能はまだたくさんあります。

しかし、このプロジェクトを通じて見えてきたことがあります。

プログラミング言語に求められる性質は、確実に変わりつつあります。パフォーマンス、明示性、予測可能性、学習可能性。これらの優先順位が、根本から変わっています。

そして、個人が「こんな言語があったらいいのに」と思ったとき、それを実現できる時代が来ています。必要なのは、ビジョンと、AI agentsとの協働スキルと、そして少しの時間です。

## 何もかもが変わろうとしています

Wadoの開発を始めたとき、「まあ、半年くらいかかるかな」と思っていました。しかし、数週間で基本機能が動き始めました。

「構文の変更は大変だろうな」と思っていました。しかし、1時間で試せるようになりました。

「ベンチマークはそこそこ速ければいいかな」と思っていました。しかし、ネイティブコードに迫る速度が出ました。

全てが、予想を超えました。良い意味で。

これからどんな言語が生まれ、どんな未来が待っているのか。私自身、とても楽しみにしています。そして、あなたも、きっと。

---

**リンク**
- Wado GitHub: https://github.com/gfx/wado
- Cheatsheet: https://github.com/gfx/wado/blob/main/docs/cheatsheet.md
- Compiler Documentation: https://github.com/gfx/wado/blob/main/docs/compiler.md
- 前回のブログ: https://gfx.hatenablog.com/entry/2025/07/06/182751

**注記**
- ベンチマーク環境: x86_64、wasmtime 40.0.2/41.0.0
- wasmサイズ比較: Rust (wasm32-wasip1)、AssemblyScript、Wado
- 開発期間: 2026年1月3日〜現在（約2週間強）
- コード量: 100%以上がAI agentsによる実装（筆者は設計とレビュー、たまにクリーンアップ）
