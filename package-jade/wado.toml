[package]
name = "jade"
description = "Jade — JSON Schema (Draft 2020-12) document model for Wado"
repository-directory = "package-jade"
lib = "src/lib.wado"
# version / repository / namespace / license / authors inherited from [workspace.package]
