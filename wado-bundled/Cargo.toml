[package]
name = "wado-bundled"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
# wado-bundled uses no_std, so we can't use workspace dependency with default-features
ryu = { version = "1.0", default-features = false }
