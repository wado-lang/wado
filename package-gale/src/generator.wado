//! Kiln generator entry for gale (stub).
//!
//! Declared by `[package].generator = "src/generator.wado"` in `wado.toml`.
//! M6.7 (WEP 2026-04-12) replaces this stub with the real implementation
//! that delegates to `./codegen.wado`.
//!
//! Until then this file only reserves the entry symbols so the package
//! manifest resolves; actual generation still goes through the
//! `gale gen` CLI in `./main.wado`.

pub struct Options {
}

export fn generate() {
    panic("gale Kiln generator is not yet implemented; use `gale gen` CLI");
}
