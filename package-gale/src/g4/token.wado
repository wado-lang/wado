pub enum G4Token {
    Grammar,
    Fragment,
    Arrow,
    Colon,
    ColonColon,
    Semicolon,
    Pipe,
    LParen,
    RParen,
    LBrace,
    RBrace,
    Comma,
    At,
    LAngle,
    RAngle,
    Star,
    Plus,
    PlusAssign,
    Question,
    Dot,
    Tilde,
    Assign,
    Hash,
    StringLit,
    CharClass,
    Identifier,
    IntLit,
    Eof,
    // Tokens the lexer could not form. Each names its own lexical problem, so
    // a parse error can report the reason without the token's text having to
    // carry it — the text stays what it is for every other kind, the source
    // slice.
    UnterminatedString,
    UnterminatedCharClass,
    UnexpectedChar,
}

impl G4Token {
    pub fn name(&self) -> String {
        return match *self {
            Grammar => "grammar",
            Fragment => "fragment",
            Arrow => "->",
            Colon => ":",
            ColonColon => "::",
            Semicolon => ";",
            Pipe => "|",
            LParen => "(",
            RParen => ")",
            LBrace => "{",
            RBrace => "}",
            Comma => ",",
            At => "@",
            LAngle => "<",
            RAngle => ">",
            Star => "*",
            Plus => "+",
            PlusAssign => "+=",
            Question => "?",
            Dot => ".",
            Tilde => "~",
            Assign => "=",
            Hash => "#",
            StringLit => "string",
            CharClass => "char-class",
            Identifier => "identifier",
            IntLit => "int",
            Eof => "EOF",
            UnterminatedString => "unterminated string",
            UnterminatedCharClass => "unterminated character set",
            UnexpectedChar => "unexpected character",
        };
    }
}
