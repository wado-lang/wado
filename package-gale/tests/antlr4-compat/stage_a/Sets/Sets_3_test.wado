#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: Sets (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: Sets/StarSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_StarSet from "../../grammars/Sets/StarSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/StarSet",
        },
    };

test "stage_a_parse: Sets/StarSet" {
    let input = "abaac";
    let result = t_StarSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeEscapedBMPRangeSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeEscapedBMPRangeSet from "../../grammars/Sets/UnicodeEscapedBMPRangeSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeEscapedBMPRangeSet",
        },
    };

test "stage_a_parse: Sets/UnicodeEscapedBMPRangeSet" {
    let input = "aáäáâåd";
    let result = t_UnicodeEscapedBMPRangeSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeEscapedBMPSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeEscapedBMPSet from "../../grammars/Sets/UnicodeEscapedBMPSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeEscapedBMPSet",
        },
    };

test "stage_a_parse: Sets/UnicodeEscapedBMPSet" {
    let input = "aäあ亜c";
    let result = t_UnicodeEscapedBMPSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeEscapedSMPRangeSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeEscapedSMPRangeSet from "../../grammars/Sets/UnicodeEscapedSMPRangeSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeEscapedSMPRangeSet",
        },
    };

test "stage_a_parse: Sets/UnicodeEscapedSMPRangeSet" {
    let input = "a😉🥂🜀d";
    let result = t_UnicodeEscapedSMPRangeSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeEscapedSMPSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeEscapedSMPSet from "../../grammars/Sets/UnicodeEscapedSMPSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeEscapedSMPSet",
        },
    };

test "stage_a_parse: Sets/UnicodeEscapedSMPSet" {
    let input = "a𝗂𝗎𝖺c";
    let result = t_UnicodeEscapedSMPSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeNegatedBMPSetIncludesSMPCodePoints (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeNegatedBMPSetIncludesSMPCodePoints from "../../grammars/Sets/UnicodeNegatedBMPSetIncludesSMPCodePoints.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeNegatedBMPSetIncludesSMPCodePoints",
        },
    };

test "stage_a_parse: Sets/UnicodeNegatedBMPSetIncludesSMPCodePoints" {
    let input = "a😳😡😝🤓c";
    let result = t_UnicodeNegatedBMPSetIncludesSMPCodePoints::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeNegatedSMPSetIncludesBMPCodePoints (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeNegatedSMPSetIncludesBMPCodePoints from "../../grammars/Sets/UnicodeNegatedSMPSetIncludesBMPCodePoints.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeNegatedSMPSetIncludesBMPCodePoints",
        },
    };

test "stage_a_parse: Sets/UnicodeNegatedSMPSetIncludesBMPCodePoints" {
    let input = "abc";
    let result = t_UnicodeNegatedSMPSetIncludesBMPCodePoints::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeUnescapedBMPRangeSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeUnescapedBMPRangeSet from "../../grammars/Sets/UnicodeUnescapedBMPRangeSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeUnescapedBMPRangeSet",
        },
    };

test "stage_a_parse: Sets/UnicodeUnescapedBMPRangeSet" {
    let input = "aáäáâåd";
    let result = t_UnicodeUnescapedBMPRangeSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: Sets/UnicodeUnescapedBMPSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_UnicodeUnescapedBMPSet from "../../grammars/Sets/UnicodeUnescapedBMPSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeUnescapedBMPSet",
        },
    };

test "stage_a_parse: Sets/UnicodeUnescapedBMPSet" {
    let input = "aäあ亜c";
    let result = t_UnicodeUnescapedBMPSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}
