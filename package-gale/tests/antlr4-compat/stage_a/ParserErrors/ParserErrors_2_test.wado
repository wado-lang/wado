#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParserErrors (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParserErrors/LL2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_LL2 from "../../grammars/ParserErrors/LL2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/LL2",
        },
    };

test "stage_a_err: ParserErrors/LL2" {
    let input = "ae";
    let result = t_LL2::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/LL3 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_LL3 from "../../grammars/ParserErrors/LL3.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/LL3",
        },
    };

test "stage_a_err: ParserErrors/LL3" {
    let input = "abe";
    let result = t_LL3::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/LLStar (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_LLStar from "../../grammars/ParserErrors/LLStar.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/LLStar",
        },
    };

test "stage_a_err: ParserErrors/LLStar" {
    let input = "aaae";
    let result = t_LLStar::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/MultiTokenDeletionBeforeLoop (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_MultiTokenDeletionBeforeLoop from "../../grammars/ParserErrors/MultiTokenDeletionBeforeLoop.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/MultiTokenDeletionBeforeLoop",
        },
    };

test "stage_a_err: ParserErrors/MultiTokenDeletionBeforeLoop" {
    let input = "aacabc";
    let result = t_MultiTokenDeletionBeforeLoop::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/MultiTokenDeletionBeforeLoop2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_MultiTokenDeletionBeforeLoop2 from "../../grammars/ParserErrors/MultiTokenDeletionBeforeLoop2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/MultiTokenDeletionBeforeLoop2",
        },
    };

test "stage_a_err: ParserErrors/MultiTokenDeletionBeforeLoop2" {
    let input = "aacabc";
    let result = t_MultiTokenDeletionBeforeLoop2::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/MultiTokenDeletionDuringLoop (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_MultiTokenDeletionDuringLoop from "../../grammars/ParserErrors/MultiTokenDeletionDuringLoop.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/MultiTokenDeletionDuringLoop",
        },
    };

test "stage_a_err: ParserErrors/MultiTokenDeletionDuringLoop" {
    let input = "abaaababc";
    let result = t_MultiTokenDeletionDuringLoop::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/MultiTokenDeletionDuringLoop2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_MultiTokenDeletionDuringLoop2 from "../../grammars/ParserErrors/MultiTokenDeletionDuringLoop2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/MultiTokenDeletionDuringLoop2",
        },
    };

test "stage_a_err: ParserErrors/MultiTokenDeletionDuringLoop2" {
    let input = "abaaababc";
    let result = t_MultiTokenDeletionDuringLoop2::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/NoViableAltAvoidance (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_NoViableAltAvoidance from "../../grammars/ParserErrors/NoViableAltAvoidance.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/NoViableAltAvoidance",
        },
    };

test "stage_a_err: ParserErrors/NoViableAltAvoidance" {
    let input = "a.";
    let result = t_NoViableAltAvoidance::parse_s(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleSetInsertion (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleSetInsertion from "../../grammars/ParserErrors/SingleSetInsertion.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleSetInsertion",
        },
    };

test "stage_a_err: ParserErrors/SingleSetInsertion" {
    let input = "ad";
    let result = t_SingleSetInsertion::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleSetInsertionConsumption (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleSetInsertionConsumption from "../../grammars/ParserErrors/SingleSetInsertionConsumption.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleSetInsertionConsumption",
        },
    };

test "stage_a_err: ParserErrors/SingleSetInsertionConsumption" {
    let input = "ad";
    let result = t_SingleSetInsertionConsumption::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}
