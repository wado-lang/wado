#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParserErrors (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParserErrors/SingleTokenDeletion (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletion from "../../grammars/ParserErrors/SingleTokenDeletion.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletion",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletion" {
    let input = "aab";
    let result = t_SingleTokenDeletion::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionBeforeAlt (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionBeforeAlt from "../../grammars/ParserErrors/SingleTokenDeletionBeforeAlt.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionBeforeAlt",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionBeforeAlt" {
    let input = "ac";
    let result = t_SingleTokenDeletionBeforeAlt::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionBeforeLoop (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionBeforeLoop from "../../grammars/ParserErrors/SingleTokenDeletionBeforeLoop.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionBeforeLoop",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionBeforeLoop" {
    let input = "aabc";
    let result = t_SingleTokenDeletionBeforeLoop::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionBeforeLoop2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionBeforeLoop2 from "../../grammars/ParserErrors/SingleTokenDeletionBeforeLoop2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionBeforeLoop2",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionBeforeLoop2" {
    let input = "aabc";
    let result = t_SingleTokenDeletionBeforeLoop2::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionBeforePredict (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionBeforePredict from "../../grammars/ParserErrors/SingleTokenDeletionBeforePredict.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionBeforePredict",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionBeforePredict" {
    let input = "caaab";
    let result = t_SingleTokenDeletionBeforePredict::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionConsumption (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionConsumption from "../../grammars/ParserErrors/SingleTokenDeletionConsumption.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionConsumption",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionConsumption" {
    let input = "aabd";
    let result = t_SingleTokenDeletionConsumption::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionDuringLoop (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionDuringLoop from "../../grammars/ParserErrors/SingleTokenDeletionDuringLoop.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionDuringLoop",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionDuringLoop" {
    let input = "ababbc";
    let result = t_SingleTokenDeletionDuringLoop::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionDuringLoop2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionDuringLoop2 from "../../grammars/ParserErrors/SingleTokenDeletionDuringLoop2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionDuringLoop2",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionDuringLoop2" {
    let input = "ababbc";
    let result = t_SingleTokenDeletionDuringLoop2::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenDeletionExpectingSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenDeletionExpectingSet from "../../grammars/ParserErrors/SingleTokenDeletionExpectingSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenDeletionExpectingSet",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenDeletionExpectingSet" {
    let input = "aab";
    let result = t_SingleTokenDeletionExpectingSet::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}

// Source descriptor: ParserErrors/SingleTokenInsertion (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_SingleTokenInsertion from "../../grammars/ParserErrors/SingleTokenInsertion.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserErrors/SingleTokenInsertion",
        },
    };

test "stage_a_err: ParserErrors/SingleTokenInsertion" {
    let input = "ac";
    let result = t_SingleTokenInsertion::parse_a(&input);
    assert !result.ok(), `expected parse to fail but it succeeded`;
}
