#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParseTrees (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParseTrees/RuleRef (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_RuleRef from "../../grammars/ParseTrees/RuleRef.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParseTrees/RuleRef",
        },
    };

test "stage_a_parse: ParseTrees/RuleRef" {
    let input = "yx";
    let result = t_RuleRef::parse_s(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParseTrees/Token2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Token2 from "../../grammars/ParseTrees/Token2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParseTrees/Token2",
        },
    };

test "stage_a_parse: ParseTrees/Token2" {
    let input = "xy";
    let result = t_Token2::parse_s(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParseTrees/TokenAndRuleContextString (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_TokenAndRuleContextString from "../../grammars/ParseTrees/TokenAndRuleContextString.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParseTrees/TokenAndRuleContextString",
        },
    };

test "stage_a_parse: ParseTrees/TokenAndRuleContextString" {
    let input = "x";
    let result = t_TokenAndRuleContextString::parse_s(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParseTrees/TwoAltLoop (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_TwoAltLoop from "../../grammars/ParseTrees/TwoAltLoop.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParseTrees/TwoAltLoop",
        },
    };

test "stage_a_parse: ParseTrees/TwoAltLoop" {
    let input = "xyyxyxz";
    let result = t_TwoAltLoop::parse_s(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParseTrees/TwoAlts (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_TwoAlts from "../../grammars/ParseTrees/TwoAlts.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParseTrees/TwoAlts",
        },
    };

test "stage_a_parse: ParseTrees/TwoAlts" {
    let input = "y";
    let result = t_TwoAlts::parse_s(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}
