#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: LexerErrors (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: LexerErrors/DFAToATNThatFailsBackToDFA (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_DFAToATNThatFailsBackToDFA from "../../grammars/LexerErrors/DFAToATNThatFailsBackToDFA.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/DFAToATNThatFailsBackToDFA",
        },
    };

test "stage_a_err: LexerErrors/DFAToATNThatFailsBackToDFA" {
    let input = "ababx";
    let tokens = t_DFAToATNThatFailsBackToDFA::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_DFAToATNThatFailsBackToDFA::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/DFAToATNThatMatchesThenFailsInATN (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_DFAToATNThatMatchesThenFailsInATN from "../../grammars/LexerErrors/DFAToATNThatMatchesThenFailsInATN.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/DFAToATNThatMatchesThenFailsInATN",
        },
    };

test "stage_a_err: LexerErrors/DFAToATNThatMatchesThenFailsInATN" {
    let input = "ababcx";
    let tokens = t_DFAToATNThatMatchesThenFailsInATN::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_DFAToATNThatMatchesThenFailsInATN::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/EnforcedGreedyNestedBraces_1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (d): tokenize(&input) renders to the same ANTLR4-format
// dump as the descriptor's [output] block.

use t_EnforcedGreedyNestedBraces_1 from "../../grammars/LexerErrors/EnforcedGreedyNestedBraces_1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/EnforcedGreedyNestedBraces_1",
        },
    };

test "stage_a_tokens: LexerErrors/EnforcedGreedyNestedBraces_1" {
    let input = "{ { } }";
    let expected = "[@0,0:6='{ { } }',<1>,1:0]
[@1,7:6='<EOF>',<-1>,1:7]
";
    let tokens = t_EnforcedGreedyNestedBraces_1::tokenize(&input);
    let actual = t_EnforcedGreedyNestedBraces_1::to_lexer_string(&tokens, t_EnforcedGreedyNestedBraces_1::TK_EOF);
    assert actual == expected, `actual:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: LexerErrors/EnforcedGreedyNestedBraces_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_EnforcedGreedyNestedBraces_2 from "../../grammars/LexerErrors/EnforcedGreedyNestedBraces_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/EnforcedGreedyNestedBraces_2",
        },
    };

test "stage_a_err: LexerErrors/EnforcedGreedyNestedBraces_2" {
    let input = "{ { }";
    let tokens = t_EnforcedGreedyNestedBraces_2::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_EnforcedGreedyNestedBraces_2::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/ErrorInMiddle (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_ErrorInMiddle from "../../grammars/LexerErrors/ErrorInMiddle.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/ErrorInMiddle",
        },
    };

test "stage_a_err: LexerErrors/ErrorInMiddle" {
    let input = "abx";
    let tokens = t_ErrorInMiddle::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_ErrorInMiddle::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/InvalidCharAtStart (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_InvalidCharAtStart from "../../grammars/LexerErrors/InvalidCharAtStart.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/InvalidCharAtStart",
        },
    };

test "stage_a_err: LexerErrors/InvalidCharAtStart" {
    let input = "x";
    let tokens = t_InvalidCharAtStart::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_InvalidCharAtStart::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/InvalidCharAtStartAfterDFACache (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_InvalidCharAtStartAfterDFACache from "../../grammars/LexerErrors/InvalidCharAtStartAfterDFACache.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/InvalidCharAtStartAfterDFACache",
        },
    };

test "stage_a_err: LexerErrors/InvalidCharAtStartAfterDFACache" {
    let input = "abx";
    let tokens = t_InvalidCharAtStartAfterDFACache::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_InvalidCharAtStartAfterDFACache::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/InvalidCharInToken (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_InvalidCharInToken from "../../grammars/LexerErrors/InvalidCharInToken.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/InvalidCharInToken",
        },
    };

test "stage_a_err: LexerErrors/InvalidCharInToken" {
    let input = "ax";
    let tokens = t_InvalidCharInToken::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_InvalidCharInToken::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/InvalidCharInTokenAfterDFACache (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_InvalidCharInTokenAfterDFACache from "../../grammars/LexerErrors/InvalidCharInTokenAfterDFACache.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/InvalidCharInTokenAfterDFACache",
        },
    };

test "stage_a_err: LexerErrors/InvalidCharInTokenAfterDFACache" {
    let input = "abax";
    let tokens = t_InvalidCharInTokenAfterDFACache::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_InvalidCharInTokenAfterDFACache::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}

// Source descriptor: LexerErrors/LexerExecDFA (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (c): the generated parser/lexer must report an error for [input].
//   - Parser-type: parse(&input) must return Err.
//   - Lexer-type:  tokenize(&input) must produce at least one TK_ERROR.

use t_LexerExecDFA from "../../grammars/LexerErrors/LexerExecDFA.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/LexerErrors/LexerExecDFA",
        },
    };

test "stage_a_err: LexerErrors/LexerExecDFA" {
    let input = "x : x";
    let tokens = t_LexerExecDFA::tokenize(&input);
    let mut has_err = false;
    for let mut i = 0; i < tokens.len(); i += 1 {
        if tokens.kinds[i] == t_LexerExecDFA::TK_ERROR {
            has_err = true;
            break;
        }
    }
    assert has_err, `expected at least one TK_ERROR token in tokenize output`;
}
