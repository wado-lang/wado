#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source descriptor: Performance/DropLoopEntryBranchInLRRule_4 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A: the generated parser must accept the descriptor's [input]
// without returning an error. No parse-tree shape comparison.

use t from "../../grammars/Performance/DropLoopEntryBranchInLRRule_4.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            options: { highlight: false },
            output_dir: "tests/generated/antlr4_compat_a/Performance/DropLoopEntryBranchInLRRule_4",
        },
    };

#[TODO]
// triage: LR expression rule with operator precedence — Gale picks the wrong precedence chain for or-then-and
test "stage_a: Performance/DropLoopEntryBranchInLRRule_4" {
    let input = "between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4 and
between X1 and X2 or between X3 and X4
;
";
    let result = t::parse(&input);
    if let Err(err) = &result {
        panic(`parse failed: {err.message} at {err.span.start}..{err.span.end}`);
    }
}
