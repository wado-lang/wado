#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParserExec (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParserExec/IfIfElseGreedyBinding1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_IfIfElseGreedyBinding1 from "../../grammars/ParserExec/IfIfElseGreedyBinding1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/IfIfElseGreedyBinding1",
        },
    };

test "stage_a_parse: ParserExec/IfIfElseGreedyBinding1" {
    let input = "if y if y x else x";
    let result = t_IfIfElseGreedyBinding1::parse_start(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/IfIfElseGreedyBinding2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_IfIfElseGreedyBinding2 from "../../grammars/ParserExec/IfIfElseGreedyBinding2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/IfIfElseGreedyBinding2",
        },
    };

test "stage_a_parse: ParserExec/IfIfElseGreedyBinding2" {
    let input = "if y if y x else x";
    let result = t_IfIfElseGreedyBinding2::parse_start(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/IfIfElseNonGreedyBinding1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_IfIfElseNonGreedyBinding1 from "../../grammars/ParserExec/IfIfElseNonGreedyBinding1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/IfIfElseNonGreedyBinding1",
        },
    };

test "stage_a_parse: ParserExec/IfIfElseNonGreedyBinding1" {
    let input = "if y if y x else x";
    let result = t_IfIfElseNonGreedyBinding1::parse_start(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/IfIfElseNonGreedyBinding2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_IfIfElseNonGreedyBinding2 from "../../grammars/ParserExec/IfIfElseNonGreedyBinding2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/IfIfElseNonGreedyBinding2",
        },
    };

test "stage_a_parse: ParserExec/IfIfElseNonGreedyBinding2" {
    let input = "if y if y x else x";
    let result = t_IfIfElseNonGreedyBinding2::parse_start(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Keyword_1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Keyword_1 from "../../grammars/ParserExec/Keyword_1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Keyword_1",
        },
    };

test "stage_a_parse: ParserExec/Keyword_1" {
    let input = "break;continue;return;";
    let result = t_Keyword_1::parse_program(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Keyword_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Keyword_2 from "../../grammars/ParserExec/Keyword_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Keyword_2",
        },
    };

test "stage_a_parse: ParserExec/Keyword_2" {
    let input = "break;continue;return;";
    let result = t_Keyword_2::parse_program(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Keyword_3 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Keyword_3 from "../../grammars/ParserExec/Keyword_3.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Keyword_3",
        },
    };

test "stage_a_parse: ParserExec/Keyword_3" {
    let input = "break;continue;return;";
    let result = t_Keyword_3::parse_program(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Keyword_4 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Keyword_4 from "../../grammars/ParserExec/Keyword_4.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Keyword_4",
        },
    };

test "stage_a_parse: ParserExec/Keyword_4" {
    let input = "break;continue;return;";
    let result = t_Keyword_4::parse_program(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Keyword_5 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Keyword_5 from "../../grammars/ParserExec/Keyword_5.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Keyword_5",
        },
    };

test "stage_a_parse: ParserExec/Keyword_5" {
    let input = "break;continue;return;";
    let result = t_Keyword_5::parse_program(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Keyword_6 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Keyword_6 from "../../grammars/ParserExec/Keyword_6.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Keyword_6",
        },
    };

test "stage_a_parse: ParserExec/Keyword_6" {
    let input = "break;continue;return;";
    let result = t_Keyword_6::parse_program(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}
