#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParserExec (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParserExec/Optional_1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Optional_1 from "../../grammars/ParserExec/Optional_1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Optional_1",
        },
    };

test "stage_a_parse: ParserExec/Optional_1" {
    let input = "x";
    let result = t_Optional_1::parse_stat(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Optional_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Optional_2 from "../../grammars/ParserExec/Optional_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Optional_2",
        },
    };

test "stage_a_parse: ParserExec/Optional_2" {
    let input = "if x";
    let result = t_Optional_2::parse_stat(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Optional_3 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Optional_3 from "../../grammars/ParserExec/Optional_3.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Optional_3",
        },
    };

test "stage_a_parse: ParserExec/Optional_3" {
    let input = "if x else x";
    let result = t_Optional_3::parse_stat(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Optional_4 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Optional_4 from "../../grammars/ParserExec/Optional_4.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Optional_4",
        },
    };

test "stage_a_parse: ParserExec/Optional_4" {
    let input = "if if x else x";
    let result = t_Optional_4::parse_stat(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/OrderingPredicates (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_OrderingPredicates from "../../grammars/ParserExec/OrderingPredicates.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/OrderingPredicates",
        },
    };

test "stage_a_parse: ParserExec/OrderingPredicates" {
    let input = "POINT AT X";
    let result = t_OrderingPredicates::parse_expr(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/PredicatedIfIfElse (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_PredicatedIfIfElse from "../../grammars/ParserExec/PredicatedIfIfElse.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/PredicatedIfIfElse",
        },
    };

test "stage_a_parse: ParserExec/PredicatedIfIfElse" {
    let input = "if x if x a else b";
    let result = t_PredicatedIfIfElse::parse_s(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/PredictionIssue334 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_PredictionIssue334 from "../../grammars/ParserExec/PredictionIssue334.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/PredictionIssue334",
        },
    };

test "stage_a_parse: ParserExec/PredictionIssue334" {
    let input = "a";
    let result = t_PredictionIssue334::parse_file_(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/PredictionMode_LL (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_PredictionMode_LL from "../../grammars/ParserExec/PredictionMode_LL.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/PredictionMode_LL",
        },
    };

test "stage_a_parse: ParserExec/PredictionMode_LL" {
    let input = "X Y";
    let result = t_PredictionMode_LL::parse_r(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/ReferenceToATN_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_ReferenceToATN_2 from "../../grammars/ParserExec/ReferenceToATN_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/ReferenceToATN_2",
        },
    };

test "stage_a_parse: ParserExec/ReferenceToATN_2" {
    let input = "a 34 c";
    let result = t_ReferenceToATN_2::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/ReservedWordsEscaping (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_ReservedWordsEscaping from "../../grammars/ParserExec/ReservedWordsEscaping.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/ReservedWordsEscaping",
        },
    };

test "stage_a_parse: ParserExec/ReservedWordsEscaping" {
    let input = "for for break break for if if for continue";
    let result = t_ReservedWordsEscaping::parse_root(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}
