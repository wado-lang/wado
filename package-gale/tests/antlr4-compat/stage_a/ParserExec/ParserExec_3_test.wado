#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParserExec (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParserExec/LL1OptionalBlock_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_LL1OptionalBlock_2 from "../../grammars/ParserExec/LL1OptionalBlock_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/LL1OptionalBlock_2",
        },
    };

test "stage_a_parse: ParserExec/LL1OptionalBlock_2" {
    let input = "a";
    let result = t_LL1OptionalBlock_2::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/LabelAliasingAcrossLabeledAlternatives (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_LabelAliasingAcrossLabeledAlternatives from "../../grammars/ParserExec/LabelAliasingAcrossLabeledAlternatives.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/LabelAliasingAcrossLabeledAlternatives",
        },
    };

test "stage_a_parse: ParserExec/LabelAliasingAcrossLabeledAlternatives" {
    let input = "xy";
    let result = t_LabelAliasingAcrossLabeledAlternatives::parse_start(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/Labels (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_Labels from "../../grammars/ParserExec/Labels.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Labels",
        },
    };

test "stage_a_parse: ParserExec/Labels" {
    let input = "abc 34;";
    let result = t_Labels::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/ListLabelForClosureContext (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_ListLabelForClosureContext from "../../grammars/ParserExec/ListLabelForClosureContext.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/ListLabelForClosureContext",
        },
    };

test "stage_a_parse: ParserExec/ListLabelForClosureContext" {
    let input = "a";
    let result = t_ListLabelForClosureContext::parse_expression(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/ListLabelsOnRuleRefStartOfAlt (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_ListLabelsOnRuleRefStartOfAlt from "../../grammars/ParserExec/ListLabelsOnRuleRefStartOfAlt.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/ListLabelsOnRuleRefStartOfAlt",
        },
    };

test "stage_a_parse: ParserExec/ListLabelsOnRuleRefStartOfAlt" {
    let input = "a and b";
    let result = t_ListLabelsOnRuleRefStartOfAlt::parse_expression(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/ListLabelsOnSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_ListLabelsOnSet from "../../grammars/ParserExec/ListLabelsOnSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/ListLabelsOnSet",
        },
    };

test "stage_a_parse: ParserExec/ListLabelsOnSet" {
    let input = "abc 34;";
    let result = t_ListLabelsOnSet::parse_a(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/MultipleEOFHandling (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_MultipleEOFHandling from "../../grammars/ParserExec/MultipleEOFHandling.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/MultipleEOFHandling",
        },
    };

test "stage_a_parse: ParserExec/MultipleEOFHandling" {
    let input = "x";
    let result = t_MultipleEOFHandling::parse_prog(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/OpenDeviceStatement_Case1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_OpenDeviceStatement_Case1 from "../../grammars/ParserExec/OpenDeviceStatement_Case1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/OpenDeviceStatement_Case1",
        },
    };

test "stage_a_parse: ParserExec/OpenDeviceStatement_Case1" {
    let input = "OPEN DEVICE DEVICE";
    let result = t_OpenDeviceStatement_Case1::parse_statement(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/OpenDeviceStatement_Case2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_OpenDeviceStatement_Case2 from "../../grammars/ParserExec/OpenDeviceStatement_Case2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/OpenDeviceStatement_Case2",
        },
    };

test "stage_a_parse: ParserExec/OpenDeviceStatement_Case2" {
    let input = "OPEN DEVICE DEVICE";
    let result = t_OpenDeviceStatement_Case2::parse_statement(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}

// Source descriptor: ParserExec/OpenDeviceStatement_Case3 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t_OpenDeviceStatement_Case3 from "../../grammars/ParserExec/OpenDeviceStatement_Case3.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/OpenDeviceStatement_Case3",
        },
    };

test "stage_a_parse: ParserExec/OpenDeviceStatement_Case3" {
    let input = "OPEN DEVICE DEVICE.";
    let result = t_OpenDeviceStatement_Case3::parse_statement(&input);
    if !result.ok() {
        let d = &result.diagnostics[0];
        panic(`parse failed: ${d.message} at ${d.span.start}..${d.span.end}`);
    }
}
