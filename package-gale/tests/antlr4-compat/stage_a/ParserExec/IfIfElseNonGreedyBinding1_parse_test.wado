#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source descriptor: ParserExec/IfIfElseNonGreedyBinding1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_a_*]).
//
// Stage A claim (b): the generated parser must accept the descriptor's
// [input] without returning an error. No parse-tree shape comparison.

use t from "../../grammars/ParserExec/IfIfElseNonGreedyBinding1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            options: { highlight: false },
            output_dir: "tests/generated/antlr4_compat_a/ParserExec/IfIfElseNonGreedyBinding1",
        },
    };

#[TODO]
// triage: parse-accepts-input: emit shape for `(elem)??` now matches the Option<T> CST decl (so the test compiles), but the parser still applies greedy `(elem)?` prediction. ANTLR4's `??` prefers not-matching when the lookahead is ambiguous; the descriptor's expected parse tree binds the `else` to the outer if. Implementing prefer-skip prediction requires either a follow-set guard on the Optional dispatch or a runtime ATN simulator (TODO.md).
test "stage_a_parse: ParserExec/IfIfElseNonGreedyBinding1" {
    let input = "if y if y x else x";
    let result = t::parse_start(&input);
    if let Err(err) = &result {
        panic(`parse failed: {err.message} at {err.span.start}..{err.span.end}`);
    }
}
