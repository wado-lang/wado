#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: LeftRecursion (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

use { normalize_tree } from "../../../support/tree_compare.wado";

// Source descriptor: LeftRecursion/JavaExpressions_1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_1 from "../../grammars/LeftRecursion/JavaExpressions_1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_1",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_1" {
    let input = "a|b&c";
    let expected = "(s (e (e a) | (e (e b) & (e c))))";
    let result = t_JavaExpressions_1::parse_s(&input);
    let actual = t_JavaExpressions_1::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_10 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_10 from "../../grammars/LeftRecursion/JavaExpressions_10.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_10",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_10" {
    let input = "a.f(x)==T.c";
    let expected = "(s (e (e (e (e a) . f) ( (expressionList (e x)) )) == (e (e T) . c)))";
    let result = t_JavaExpressions_10::parse_s(&input);
    let actual = t_JavaExpressions_10::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_11 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_11 from "../../grammars/LeftRecursion/JavaExpressions_11.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_11",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_11" {
    let input = "a.f().g(x,1)";
    let expected = "(s (e (e (e (e (e a) . f) ( )) . g) ( (expressionList (e x) , (e 1)) )))";
    let result = t_JavaExpressions_11::parse_s(&input);
    let actual = t_JavaExpressions_11::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_12 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_12 from "../../grammars/LeftRecursion/JavaExpressions_12.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_12",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_12" {
    let input = "new T[((n-1) * x) + 1]";
    let expected = "(s (e new (typespec T) [ (e (e ( (e (e ( (e (e n) - (e 1)) )) * (e x)) )) + (e 1)) ]))";
    let result = t_JavaExpressions_12::parse_s(&input);
    let actual = t_JavaExpressions_12::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_2 from "../../grammars/LeftRecursion/JavaExpressions_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_2",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_2" {
    let input = "(a|b)&c";
    let expected = "(s (e (e ( (e (e a) | (e b)) )) & (e c)))";
    let result = t_JavaExpressions_2::parse_s(&input);
    let actual = t_JavaExpressions_2::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_5 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_5 from "../../grammars/LeftRecursion/JavaExpressions_5.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_5",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_5" {
    let input = "a=b=c";
    let expected = "(s (e (e a) = (e (e b) = (e c))))";
    let result = t_JavaExpressions_5::parse_s(&input);
    let actual = t_JavaExpressions_5::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_6 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_6 from "../../grammars/LeftRecursion/JavaExpressions_6.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_6",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_6" {
    let input = "a^b^c";
    let expected = "(s (e (e a) ^ (e (e b) ^ (e c))))";
    let result = t_JavaExpressions_6::parse_s(&input);
    let actual = t_JavaExpressions_6::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_7 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_7 from "../../grammars/LeftRecursion/JavaExpressions_7.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_7",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_7" {
    let input = "(T)x";
    let expected = "(s (e ( (typespec T) ) (e x)))";
    let result = t_JavaExpressions_7::parse_s(&input);
    let actual = t_JavaExpressions_7::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_8 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_8 from "../../grammars/LeftRecursion/JavaExpressions_8.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_8",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_8" {
    let input = "new A().b";
    let expected = "(s (e (e new (typespec A) ( )) . b))";
    let result = t_JavaExpressions_8::parse_s(&input);
    let actual = t_JavaExpressions_8::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: LeftRecursion/JavaExpressions_9 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).

use t_JavaExpressions_9 from "../../grammars/LeftRecursion/JavaExpressions_9.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/LeftRecursion/JavaExpressions_9",
        },
    };

test "stage_b: LeftRecursion/JavaExpressions_9" {
    let input = "(T)t.f()";
    let expected = "(s (e (e ( (typespec T) ) (e (e t) . f)) ( )))";
    let result = t_JavaExpressions_9::parse_s(&input);
    let actual = t_JavaExpressions_9::to_string_tree(&result);
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}
