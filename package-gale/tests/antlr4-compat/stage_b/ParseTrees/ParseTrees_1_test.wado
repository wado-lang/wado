#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParseTrees (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

use { normalize_tree, strip_skip_markers } from "../../../support/tree_compare.wado";

// Source descriptor: ParseTrees/ExtraToken (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.
//
// The descriptor deletes a token, which ANTLR4 renders bare and Gale
// wraps in `<skip …>`. `strip_skip_markers` drops the wrapper (not the
// token) so the compare is over the recovery decision, not the marker.

use t_ExtraToken from "../../grammars/ParseTrees/ExtraToken.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/ParseTrees/ExtraToken",
        },
    };

test "stage_b_sub: ParseTrees/ExtraToken" {
    let input = "xzy";
    let expected = "(a x z y)";
    let result = t_ExtraToken::parse_s(&input);
    let actual = strip_skip_markers(&t_ExtraToken::to_string_subtree(&result, &"a"));
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: ParseTrees/NoViableAlt (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.

use t_NoViableAlt from "../../grammars/ParseTrees/NoViableAlt.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/ParseTrees/NoViableAlt",
        },
    };

// triage: sub-tree: expected [output] is ANTLR4's error-recovered sub-tree (NoViableAlternativeException fallback). Gale ends the rule at the last good token and renders a bare `a`, dropping the tokens ANTLR4 keeps under the node.
#[TODO]
test "stage_b_sub: ParseTrees/NoViableAlt" {
    let input = "z";
    let expected = "(a z)";
    let result = t_NoViableAlt::parse_s(&input);
    let actual = t_NoViableAlt::to_string_subtree(&result, &"a");
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: ParseTrees/RuleRef (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.

use t_RuleRef from "../../grammars/ParseTrees/RuleRef.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/ParseTrees/RuleRef",
        },
    };

test "stage_b_sub: ParseTrees/RuleRef" {
    let input = "yx";
    let expected = "(a (b y) x)";
    let result = t_RuleRef::parse_s(&input);
    let actual = t_RuleRef::to_string_subtree(&result, &"a");
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: ParseTrees/Sync (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.
//
// The descriptor deletes a token, which ANTLR4 renders bare and Gale
// wraps in `<skip …>`. `strip_skip_markers` drops the wrapper (not the
// token) so the compare is over the recovery decision, not the marker.

use t_Sync from "../../grammars/ParseTrees/Sync.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/ParseTrees/Sync",
        },
    };

// triage: sub-tree: expected [output] is ANTLR4's error-recovered sub-tree (synchronisation set fallback). Gale stops at `(a x)` instead of consuming to the synchronisation point, so the trailing tokens never land under the node.
#[TODO]
test "stage_b_sub: ParseTrees/Sync" {
    let input = "xzyy!";
    let expected = "(a x z y y !)";
    let result = t_Sync::parse_s(&input);
    let actual = strip_skip_markers(&t_Sync::to_string_subtree(&result, &"a"));
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: ParseTrees/Token2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.

use t_Token2 from "../../grammars/ParseTrees/Token2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/ParseTrees/Token2",
        },
    };

test "stage_b_sub: ParseTrees/Token2" {
    let input = "xy";
    let expected = "(a x y)";
    let result = t_Token2::parse_s(&input);
    let actual = t_Token2::to_string_subtree(&result, &"a");
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: ParseTrees/TwoAltLoop (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.

use t_TwoAltLoop from "../../grammars/ParseTrees/TwoAltLoop.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/ParseTrees/TwoAltLoop",
        },
    };

test "stage_b_sub: ParseTrees/TwoAltLoop" {
    let input = "xyyxyxz";
    let expected = "(a x y y x y x z)";
    let result = t_TwoAltLoop::parse_s(&input);
    let actual = t_TwoAltLoop::to_string_subtree(&result, &"a");
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}

// Source descriptor: ParseTrees/TwoAlts (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.

use t_TwoAlts from "../../grammars/ParseTrees/TwoAlts.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_b/ParseTrees/TwoAlts",
        },
    };

test "stage_b_sub: ParseTrees/TwoAlts" {
    let input = "y";
    let expected = "(a y)";
    let result = t_TwoAlts::parse_s(&input);
    let actual = t_TwoAlts::to_string_subtree(&result, &"a");
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   ${actual}\nexpected: ${norm}`;
}
