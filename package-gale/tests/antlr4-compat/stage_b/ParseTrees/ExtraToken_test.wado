#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source descriptor: ParseTrees/ExtraToken (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_*]).
//
// Stage B sub-tree: [output] is the labelled child 'a' subtree,
// not the start rule's full tree. Navigate via find_first_child_node.

use t from "../../grammars/ParseTrees/ExtraToken.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            options: { highlight: false },
            output_dir: "tests/generated/antlr4_compat_b/ParseTrees/ExtraToken",
        },
    };
use { normalize_tree } from "../../grammars/ParseTrees/ExtraToken.g4";

#[TODO]
// triage: sub-tree: expected [output] is ANTLR4's error-recovered sub-tree (single-token deletion / insertion). Gale does not yet implement error recovery, so `parse()` returns `Err` on the bad token and the sub-tree navigation never runs. Gap: implement ANTLR4-style error recovery in the generated parser. (Stage C-independent)
test "stage_b_sub: ParseTrees/ExtraToken" {
    let input = "xzy";
    let expected = "(a x z y)";
    let root = t::parse_s(&input).unwrap();
    let tree = t::to_tree(&root);
    let sub = t::find_first_child_node(&tree, &"a").unwrap();
    let actual = sub.to_string_tree();
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   {actual}\nexpected: {norm}`;
}
