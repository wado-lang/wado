#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: SemPredEvalParser (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: SemPredEvalParser/Simple (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_Simple from "../../grammars/SemPredEvalParser/Simple.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/SemPredEvalParser/Simple",
        },
    };

test "stage_c_output: SemPredEvalParser/Simple" {
    let input = "x y 3";
    let expected = "alt 2
alt 2
alt 3
";
    let result = t_Simple::parse_s(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: SemPredEvalParser/SimpleValidate2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_SimpleValidate2 from "../../grammars/SemPredEvalParser/SimpleValidate2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/SemPredEvalParser/SimpleValidate2",
        },
    };

test "stage_c_output: SemPredEvalParser/SimpleValidate2" {
    let input = "3 4 x";
    let expected = "alt 2
alt 2
";
    let result = t_SimpleValidate2::parse_s(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: SemPredEvalParser/ToLeft (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ToLeft from "../../grammars/SemPredEvalParser/ToLeft.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/SemPredEvalParser/ToLeft",
        },
    };

test "stage_c_output: SemPredEvalParser/ToLeft" {
    let input = "x x y";
    let expected = "alt 2
alt 2
alt 2
";
    let result = t_ToLeft::parse_s(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: SemPredEvalParser/ToLeftWithVaryingPredicate (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ToLeftWithVaryingPredicate from "../../grammars/SemPredEvalParser/ToLeftWithVaryingPredicate.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/SemPredEvalParser/ToLeftWithVaryingPredicate",
        },
    };

test "stage_c_output: SemPredEvalParser/ToLeftWithVaryingPredicate" {
    let input = "x x y";
    let expected = "i=1
alt 2
i=2
alt 1
i=3
alt 2
";
    let result = t_ToLeftWithVaryingPredicate::parse_s(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: SemPredEvalParser/TwoUnpredicatedAlts (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_TwoUnpredicatedAlts from "../../grammars/SemPredEvalParser/TwoUnpredicatedAlts.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/SemPredEvalParser/TwoUnpredicatedAlts",
        },
    };

test "stage_c_output: SemPredEvalParser/TwoUnpredicatedAlts" {
    let input = "x; y";
    let expected = "alt 1
alt 1
";
    let result = t_TwoUnpredicatedAlts::parse_s(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: SemPredEvalParser/TwoUnpredicatedAltsAndOneOrthogonalAlt (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_TwoUnpredicatedAltsAndOneOrthogonalAlt from "../../grammars/SemPredEvalParser/TwoUnpredicatedAltsAndOneOrthogonalAlt.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/SemPredEvalParser/TwoUnpredicatedAltsAndOneOrthogonalAlt",
        },
    };

test "stage_c_output: SemPredEvalParser/TwoUnpredicatedAltsAndOneOrthogonalAlt" {
    let input = "34; x; y";
    let expected = "alt 1
alt 2
alt 2
";
    let result = t_TwoUnpredicatedAltsAndOneOrthogonalAlt::parse_s(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: SemPredEvalParser/UnpredicatedPathsInAlt (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_UnpredicatedPathsInAlt from "../../grammars/SemPredEvalParser/UnpredicatedPathsInAlt.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/SemPredEvalParser/UnpredicatedPathsInAlt",
        },
    };

test "stage_c_output: SemPredEvalParser/UnpredicatedPathsInAlt" {
    let input = "x 4";
    let expected = "alt 1
";
    let result = t_UnpredicatedPathsInAlt::parse_s(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}
