#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParserExec (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParserExec/OpenDeviceStatement_Case1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_OpenDeviceStatement_Case1 from "../../grammars/ParserExec/OpenDeviceStatement_Case1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/OpenDeviceStatement_Case1",
        },
    };

test "stage_c_output: ParserExec/OpenDeviceStatement_Case1" {
    let input = "OPEN DEVICE DEVICE";
    let expected = "OPEN DEVICE DEVICE
";
    let result = t_OpenDeviceStatement_Case1::parse_statement(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/OpenDeviceStatement_Case2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_OpenDeviceStatement_Case2 from "../../grammars/ParserExec/OpenDeviceStatement_Case2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/OpenDeviceStatement_Case2",
        },
    };

test "stage_c_output: ParserExec/OpenDeviceStatement_Case2" {
    let input = "OPEN DEVICE DEVICE";
    let expected = "OPEN DEVICE DEVICE
";
    let result = t_OpenDeviceStatement_Case2::parse_statement(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/OpenDeviceStatement_Case3 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_OpenDeviceStatement_Case3 from "../../grammars/ParserExec/OpenDeviceStatement_Case3.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/OpenDeviceStatement_Case3",
        },
    };

test "stage_c_output: ParserExec/OpenDeviceStatement_Case3" {
    let input = "OPEN DEVICE DEVICE.";
    let expected = "OPEN DEVICE DEVICE
";
    let result = t_OpenDeviceStatement_Case3::parse_statement(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/PredictionMode_SLL (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_PredictionMode_SLL from "../../grammars/ParserExec/PredictionMode_SLL.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/PredictionMode_SLL",
        },
    };

test "stage_c_output: ParserExec/PredictionMode_SLL" {
    let input = "X Y";
    let expected = "XY
";
    let result = t_PredictionMode_SLL::parse_r(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/ReferenceToATN_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ReferenceToATN_2 from "../../grammars/ParserExec/ReferenceToATN_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/ReferenceToATN_2",
        },
    };

test "stage_c_output: ParserExec/ReferenceToATN_2" {
    let input = "a 34 c";
    let expected = "a34c
";
    let result = t_ReferenceToATN_2::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/ReservedWordsEscaping (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ReservedWordsEscaping from "../../grammars/ParserExec/ReservedWordsEscaping.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/ReservedWordsEscaping",
        },
    };

test "stage_c_output: ParserExec/ReservedWordsEscaping" {
    let input = "for for break break for if if for continue";
    let expected = "for for break break for if if for continue";
    let result = t_ReservedWordsEscaping::parse_root(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/TokenOffset (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_TokenOffset from "../../grammars/ParserExec/TokenOffset.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/TokenOffset",
        },
    };

test "stage_c_output: ParserExec/TokenOffset" {
    let input = "12 34 56 66";
    let expected = "12345666
";
    let result = t_TokenOffset::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/Wildcard (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_Wildcard from "../../grammars/ParserExec/Wildcard.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Wildcard",
        },
    };

test "stage_c_output: ParserExec/Wildcard" {
    let input = "x=10; abc;;;; y=99;";
    let expected = "x=10;
y=99;
";
    let result = t_Wildcard::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/uStartingCharDoesNotCauseIllegalUnicodeEscape (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_uStartingCharDoesNotCauseIllegalUnicodeEscape from "../../grammars/ParserExec/uStartingCharDoesNotCauseIllegalUnicodeEscape.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/uStartingCharDoesNotCauseIllegalUnicodeEscape",
        },
    };

test "stage_c_output: ParserExec/uStartingCharDoesNotCauseIllegalUnicodeEscape" {
    let input = "u";
    let expected = "u
";
    let result = t_uStartingCharDoesNotCauseIllegalUnicodeEscape::parse_u(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}
