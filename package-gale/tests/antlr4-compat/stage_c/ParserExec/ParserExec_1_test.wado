#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: ParserExec (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: ParserExec/APlus (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_APlus from "../../grammars/ParserExec/APlus.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/APlus",
        },
    };

test "stage_c_output: ParserExec/APlus" {
    let input = "a b c";
    let expected = "abc
";
    let result = t_APlus::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/AStar_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_AStar_2 from "../../grammars/ParserExec/AStar_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/AStar_2",
        },
    };

test "stage_c_output: ParserExec/AStar_2" {
    let input = "a b c";
    let expected = "abc
";
    let result = t_AStar_2::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/AorAPlus (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_AorAPlus from "../../grammars/ParserExec/AorAPlus.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/AorAPlus",
        },
    };

test "stage_c_output: ParserExec/AorAPlus" {
    let input = "a b c";
    let expected = "abc
";
    let result = t_AorAPlus::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/AorAStar_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_AorAStar_2 from "../../grammars/ParserExec/AorAStar_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/AorAStar_2",
        },
    };

test "stage_c_output: ParserExec/AorAStar_2" {
    let input = "a b c";
    let expected = "abc
";
    let result = t_AorAStar_2::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/AorB (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_AorB from "../../grammars/ParserExec/AorB.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/AorB",
        },
    };

test "stage_c_output: ParserExec/AorB" {
    let input = "34";
    let expected = "alt 2
";
    let result = t_AorB::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/AorBPlus (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_AorBPlus from "../../grammars/ParserExec/AorBPlus.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/AorBPlus",
        },
    };

test "stage_c_output: ParserExec/AorBPlus" {
    let input = "a 34 c";
    let expected = "a34c
";
    let result = t_AorBPlus::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/AorBStar_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_AorBStar_2 from "../../grammars/ParserExec/AorBStar_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/AorBStar_2",
        },
    };

test "stage_c_output: ParserExec/AorBStar_2" {
    let input = "a 34 c";
    let expected = "a34c
";
    let result = t_AorBStar_2::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/Basic (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_Basic from "../../grammars/ParserExec/Basic.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/Basic",
        },
    };

test "stage_c_output: ParserExec/Basic" {
    let input = "abc 34";
    let expected = "abc34
";
    let result = t_Basic::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/IfIfElseGreedyBinding1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_IfIfElseGreedyBinding1 from "../../grammars/ParserExec/IfIfElseGreedyBinding1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/IfIfElseGreedyBinding1",
        },
    };

test "stage_c_output: ParserExec/IfIfElseGreedyBinding1" {
    let input = "if y if y x else x";
    let expected = "if y x else x
if y if y x else x
";
    let result = t_IfIfElseGreedyBinding1::parse_start(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: ParserExec/IfIfElseGreedyBinding2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_IfIfElseGreedyBinding2 from "../../grammars/ParserExec/IfIfElseGreedyBinding2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/ParserExec/IfIfElseGreedyBinding2",
        },
    };

test "stage_c_output: ParserExec/IfIfElseGreedyBinding2" {
    let input = "if y if y x else x";
    let expected = "if y x else x
if y if y x else x
";
    let result = t_IfIfElseGreedyBinding2::parse_start(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}
