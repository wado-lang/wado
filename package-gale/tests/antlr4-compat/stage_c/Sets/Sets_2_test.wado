#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: Sets (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: Sets/ParserNotSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ParserNotSet from "../../grammars/Sets/ParserNotSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/ParserNotSet",
        },
    };

test "stage_c_output: Sets/ParserNotSet" {
    let input = "zz";
    let expected = "z
";
    let result = t_ParserNotSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/ParserNotToken (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ParserNotToken from "../../grammars/Sets/ParserNotToken.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/ParserNotToken",
        },
    };

test "stage_c_output: Sets/ParserNotToken" {
    let input = "zz";
    let expected = "zz
";
    let result = t_ParserNotToken::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/ParserNotTokenWithLabel (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ParserNotTokenWithLabel from "../../grammars/Sets/ParserNotTokenWithLabel.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/ParserNotTokenWithLabel",
        },
    };

test "stage_c_output: Sets/ParserNotTokenWithLabel" {
    let input = "zz";
    let expected = "z
";
    let result = t_ParserNotTokenWithLabel::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/ParserSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_ParserSet from "../../grammars/Sets/ParserSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/ParserSet",
        },
    };

test "stage_c_output: Sets/ParserSet" {
    let input = "x";
    let expected = "x
";
    let result = t_ParserSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/PlusLexerSingleElement (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_PlusLexerSingleElement from "../../grammars/Sets/PlusLexerSingleElement.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/PlusLexerSingleElement",
        },
    };

test "stage_c_output: Sets/PlusLexerSingleElement" {
    let input = "bbbbc";
    let expected = "bbbbc
";
    let result = t_PlusLexerSingleElement::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/PlusSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_PlusSet from "../../grammars/Sets/PlusSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/PlusSet",
        },
    };

test "stage_c_output: Sets/PlusSet" {
    let input = "abaac";
    let expected = "abaac
";
    let result = t_PlusSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/RuleAsSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_RuleAsSet from "../../grammars/Sets/RuleAsSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/RuleAsSet",
        },
    };

test "stage_c_output: Sets/RuleAsSet" {
    let input = "b";
    let expected = "b
";
    let result = t_RuleAsSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/SeqDoesNotBecomeSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_SeqDoesNotBecomeSet from "../../grammars/Sets/SeqDoesNotBecomeSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/SeqDoesNotBecomeSet",
        },
    };

test "stage_c_output: Sets/SeqDoesNotBecomeSet" {
    let input = "34";
    let expected = "34
";
    let result = t_SeqDoesNotBecomeSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/StarLexerSingleElement_1 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_StarLexerSingleElement_1 from "../../grammars/Sets/StarLexerSingleElement_1.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/StarLexerSingleElement_1",
        },
    };

test "stage_c_output: Sets/StarLexerSingleElement_1" {
    let input = "bbbbc";
    let expected = "bbbbc
";
    let result = t_StarLexerSingleElement_1::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/StarLexerSingleElement_2 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_StarLexerSingleElement_2 from "../../grammars/Sets/StarLexerSingleElement_2.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/StarLexerSingleElement_2",
        },
    };

test "stage_c_output: Sets/StarLexerSingleElement_2" {
    let input = "c";
    let expected = "c
";
    let result = t_StarLexerSingleElement_2::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}
