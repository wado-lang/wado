#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source category: Sets (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml

// Source descriptor: Sets/CharSetLiteral (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_CharSetLiteral from "../../grammars/Sets/CharSetLiteral.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/CharSetLiteral",
        },
    };

test "stage_c_output: Sets/CharSetLiteral" {
    let input = "A a B b";
    let expected = "A
a
B
b
";
    let result = t_CharSetLiteral::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/LexerOptionalSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_LexerOptionalSet from "../../grammars/Sets/LexerOptionalSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/LexerOptionalSet",
        },
    };

test "stage_c_output: Sets/LexerOptionalSet" {
    let input = "ac";
    let expected = "ac
";
    let result = t_LexerOptionalSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/LexerPlusSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_LexerPlusSet from "../../grammars/Sets/LexerPlusSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/LexerPlusSet",
        },
    };

test "stage_c_output: Sets/LexerPlusSet" {
    let input = "abaac";
    let expected = "abaac
";
    let result = t_LexerPlusSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/LexerStarSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_LexerStarSet from "../../grammars/Sets/LexerStarSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/LexerStarSet",
        },
    };

test "stage_c_output: Sets/LexerStarSet" {
    let input = "abaac";
    let expected = "abaac
";
    let result = t_LexerStarSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/NotChar (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_NotChar from "../../grammars/Sets/NotChar.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/NotChar",
        },
    };

test "stage_c_output: Sets/NotChar" {
    let input = "x";
    let expected = "x
";
    let result = t_NotChar::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/NotCharSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_NotCharSet from "../../grammars/Sets/NotCharSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/NotCharSet",
        },
    };

test "stage_c_output: Sets/NotCharSet" {
    let input = "x";
    let expected = "x
";
    let result = t_NotCharSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/NotCharSetWithRuleRef3 (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_NotCharSetWithRuleRef3 from "../../grammars/Sets/NotCharSetWithRuleRef3.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/NotCharSetWithRuleRef3",
        },
    };

test "stage_c_output: Sets/NotCharSetWithRuleRef3" {
    let input = "x";
    let expected = "x
";
    let result = t_NotCharSetWithRuleRef3::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/OptionalLexerSingleElement (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_OptionalLexerSingleElement from "../../grammars/Sets/OptionalLexerSingleElement.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/OptionalLexerSingleElement",
        },
    };

test "stage_c_output: Sets/OptionalLexerSingleElement" {
    let input = "bc";
    let expected = "bc
";
    let result = t_OptionalLexerSingleElement::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/OptionalSet (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_OptionalSet from "../../grammars/Sets/OptionalSet.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/OptionalSet",
        },
    };

test "stage_c_output: Sets/OptionalSet" {
    let input = "ac";
    let expected = "ac
";
    let result = t_OptionalSet::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}

// Source descriptor: Sets/OptionalSingleElement (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t_OptionalSingleElement from "../../grammars/Sets/OptionalSingleElement.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/OptionalSingleElement",
        },
    };

test "stage_c_output: Sets/OptionalSingleElement" {
    let input = "bc";
    let expected = "bc
";
    let result = t_OptionalSingleElement::parse_a(&input);
    let actual = result.output;
    assert actual == expected, `output:\n${actual}\nexpected:\n${expected}`;
}
