#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source descriptor: Sets/UnicodeEscapedSMPRangeSetMismatch (ANTLR4 runtime-testsuite).
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_c_*]).
//
// Stage C claim: the generated parser's action-print output
// (result.output, the p.emit buffer) equals the descriptor's [output].

use t from "../../grammars/Sets/UnicodeEscapedSMPRangeSetMismatch.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            output_dir: "../../../generated/antlr4_compat_a/Sets/UnicodeEscapedSMPRangeSetMismatch",
        },
    };

// triage: the printed `$text` includes the actual SMP code points Gale matched, while the descriptor [output] elides them; non-ASCII action-print divergence, same family as the Unicode oracle-encoding skips.
#[TODO]
test "stage_c_output: Sets/UnicodeEscapedSMPRangeSetMismatch" {
    let input = "a🗿🥄d";
    let expected = "ad
";
    let result = t::parse_a(&input);
    assert result.output == expected, `output:\n${result.output}\nexpected:\n${expected}`;
}
