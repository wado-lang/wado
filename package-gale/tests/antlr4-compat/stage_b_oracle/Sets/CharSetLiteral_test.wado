#![generated(by = "package-gale/scripts/extract_antlr4_descriptors.wado --finalize-stage-b-oracle")]
// Do not edit by hand — re-run the generator to regenerate.
//
// Source descriptor: Sets/CharSetLiteral (ANTLR4 runtime-testsuite).
// Expected tree: ANTLR4 oracle (antlr-<unspecified>-complete.jar) on the
//   action-body-stripped grammar — see package-gale/CLAUDE.md
//   "Stage B′" for the rationale.
// Triage data: package-gale/tests/antlr4-compat/status.toml ([stage_b_oracle_*]).

use t from "../../grammars/Sets/CharSetLiteral.g4"
    with {
        generator: {
            module: "../../../../src/generator.wado",
            options: { highlight: false },
            output_dir: "tests/generated/antlr4_compat_b_oracle/Sets/CharSetLiteral",
        },
    };
use { normalize_tree } from "../../grammars/Sets/CharSetLiteral.g4";

#[TODO]
// triage: Lexer or grammar shape mismatch — Gale produces `(a)` while the oracle produces `(a A a B b)`. Likely a Gale-side parser bug (rule fails to repeat or EOF is consumed too early). Worth a TDD investigation as a self-contained ATN-class case.
test "stage_b_oracle: Sets/CharSetLiteral" {
    let input = "A a B b";
    let expected = "(a A a B b)";
    let root = t::parse_a(&input).unwrap();
    let actual = t::to_tree(&root).to_string_tree();
    let norm = normalize_tree(&expected);
    assert actual == norm, `actual:   {actual}\nexpected: {norm}`;
}
