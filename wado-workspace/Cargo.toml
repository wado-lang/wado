[package]
name = "wado-workspace"
version = { workspace = true }
edition = "2024"
description = "Filesystem-backed workspace and dependency discovery for Wado projects"
license = { workspace = true }
rust-version = { workspace = true }

[dependencies]
wado-manifest = { path = "../wado-manifest" }
glob = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
