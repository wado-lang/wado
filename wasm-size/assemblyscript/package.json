{
  "name": "wasm-size-assemblyscript",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:hello": "asc hello_world.ts -o hello_world.wasm --optimize --optimizeLevel 3 --shrinkLevel 2",
    "build:pi": "asc pi_approx.ts -o pi_approx.wasm --optimize --optimizeLevel 3 --shrinkLevel 2"
  },
  "devDependencies": {
    "@assemblyscript/wasi-shim": "^0.1.0",
    "assemblyscript": "^0.28.9"
  }
}
