name = "wado-http-bin"
main = "worker.mjs"
compatibility_date = "2026-08-01"
# `preview3-shim`'s Fields imports node:http.
compatibility_flags = ["nodejs_compat"]

# gen/ carries the component as two core modules.
[[rules]]
type = "CompiledWasm"
globs = ["**/*.wasm"]
