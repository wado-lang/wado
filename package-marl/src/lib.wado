//! Marl — Markdown Adaptive Rendering Layer.
//!
//! A GFM-subset Markdown-to-HTML renderer. The public surface is a single pure
//! function, `render`, plus the HTML escaping helpers it is built on.

pub use { render } from "./marl.wado";
pub use { escape_text, escape_attr } from "./html.wado";
