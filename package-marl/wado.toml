[package]
name = "marl"
description = "A GFM-subset Markdown-to-HTML renderer and formatter written in Wado"
repository-directory = "package-marl"
lib = "src/lib.wado"
# version / repository / namespace / license / authors inherited from [workspace.package]

[world]
"wasi:cli/command" = "src/main.wado"
