[package]
name = "geas"
description = "Geas — tensor compilation for Wado: shapes checked at build time"
repository-directory = "package-geas"
lib = "src/lib.wado"
# version / repository / namespace / license / authors inherited from [workspace.package]

[world]
"core:kiln/generator" = "src/generator.wado"
