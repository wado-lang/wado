#![no_prelude]
#![stdlib("core:prelude")]
//! Auto-imported in every module. Disable with `#![no_prelude]`.


// The Wado Prelude Module.
// Auto-imported in every module. Disable with: #![no_prelude]

pub use {
    Ordering,
    CaseStyle,
    Eq,
    Ord,
    IndexRef,
    IndexMutRef,
    IndexAssign,
    IndexValue,
    Add,
    Sub,
    Mul,
    Div,
    Rem,
    Neg,
    BitAnd,
    BitOr,
    BitXor,
    BitNot,
    Shl,
    Shr,
    Iterator,
    IntoIterator,
    FromIterator,
    ConvertError,
    Display,
    Binary,
    Octal,
    LowerHex,
    UpperHex,
    LowerExp,
    UpperExp,
} from "core:prelude/traits.wado";

pub use { Alignment, Formatter, PushDisplay } from "core:prelude/format.wado";
pub use { i128, u128 } from "core:prelude/int128.wado";
pub use {
    Option,
    Result,
    Stream,
    StreamWritable,
    Future,
    FutureWritable,
    Waitable,
    WaitEvent,
    WaitableSet,
    Subtask,
    AsyncCall,
    Task,
    ErrorContext,
} from "core:prelude/types.wado";
use _ from "core:prelude/fpfmt.wado";
pub use { IntErrorKind, ParseIntError, FloatErrorKind, ParseFloatError } from "core:prelude/intparse.wado";
use _ from "core:prelude/primitive.wado";
pub use { Array } from "core:prelude/array.wado";
pub use { ArraySlice, ArrayIter, ArrayRefIter, ArrayWindows, ArrayChunks } from "core:prelude/slice.wado";
pub use { String, StrUtf8ByteIter, StrCharIter } from "core:prelude/string.wado";
pub use { IterMap, IterFilter, IterEnumerate, IterTake, IterSkip, IterChain, IterZip } from "core:prelude/traits.wado";
pub use { List } from "core:prelude/list.wado";
pub use { ByteArray, ByteList, ByteSlice, AsByteSlice } from "core:prelude/bytes.wado";
use _ from "core:prelude/tuple.wado";
pub use { Step, RangeExclusive, RangeInclusive } from "core:prelude/range.wado";
pub use { panic, unreachable } from "core:rt";

#[synopsis]
test {
    let mut greeting = String::new();
    greeting.push_str(&"Hello, ");
    greeting.push_str(&"Wado!");
    assert greeting == "Hello, Wado!";

    let primes = [2, 3, 5, 7] as List<i32>;
    assert primes.len() == 4;
    assert primes.get(1) matches { Some(v) && v == 3 };
    assert primes.get(9) matches { None };

    let decoded = String::from_utf8([87, 97, 100, 111] as List<u8>);
    assert decoded matches { Ok(s) && s == "Wado" };
}
