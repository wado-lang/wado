// A gap before the backtick leaves the literal an unexpected token inside an
// argument list too. The message names the token by its kind, not its parts.
use { println } from "core:cli";

export fn run() with Stdout {
    let x = 1;
    println(format `a ${x} b`);
}

__DATA__
{"compile_error": ":7:20: parse error: expected `)`, found a template literal"}
