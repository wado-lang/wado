use { println, Stdout } from "core:cli";

export fn run() with Stdout {
    let owned = "49".as_bytes().to_list();
    let view = owned as Slice<u8>;
    println(`${view.len()}`);
}

__DATA__
{"compile_error": "cannot cast 'List<u8>' to 'Slice<u8>': a slice is a view and a `List` owns its elements; use `.as_slice()` instead"}
