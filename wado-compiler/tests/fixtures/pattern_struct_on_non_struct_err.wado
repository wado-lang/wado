// A struct pattern needs a struct to destructure, in a `match` arm as in a
// `let`, even when it names no field to look up.
export fn run() {
    let x = builtin::black_box(5);
    let n = match x {
        {} && false => 1,
        _ => 0,
    };
    if let { a } = x {
        let _ = a;
    }
}

__DATA__
{
    "compile_error": ":6:9: analysis error: pattern mismatch: expected 'struct type', found 'i32'; 9:12: pattern mismatch: expected 'struct type', found 'i32'",
    "compile_errors_not_contains": ["unknown identifier"]
}
