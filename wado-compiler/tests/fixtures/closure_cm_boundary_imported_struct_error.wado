// Cross-module CM boundary: a struct defined in another module with a
// closure-typed field still trips the boundary check when an export
// function references it. `type_contains_closure` consults the
// elaborator's `all_struct_fields` registry, which is populated for all
// loaded modules — not just the entry module — so the recursion works
// across module boundaries.

use { ImportedHolder } from "./sub/closure_cm_boundary_imported_struct_helper.wado";

export fn run(h: ImportedHolder) {
    let _ = h;
}

__DATA__
{"compile_error": "closure"}
