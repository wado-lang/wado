// Test: Stream<T> and Future<T> type declarations
use { println, Stdout } from "core:cli";

// Declare function parameters with Stream and Future types
fn takes_stream(s: Stream<u8>) {
    // placeholder - just verify the type exists
}

fn takes_future(f: Future<i32>) {
    // placeholder - just verify the type exists
}

export fn run() with Stdout {
    println("Stream and Future types exist");
}

__DATA__
{"stdout": "Stream and Future types exist\n"}
