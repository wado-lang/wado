// Range patterns with associated constants

test "range pattern with i32 MIN/MAX" {
    let n: i32 = -5;
    let sign = match n {
        i32::MIN..<0 => "negative",
        0 => "zero",
        1..=i32::MAX => "positive",
    };
    assert sign == "negative";
}
