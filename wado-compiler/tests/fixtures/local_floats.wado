// Test local floating point variables

use {println, Stdout} from "core:cli";

fn main() with Stdout {
    let a = 1.5;
    let b = 2.5;
    let sum = a + b;

    // Test: sum should be 4.0
    if sum == 4.0 {
        println("floats work");
    }
}
