//! `internal use` re-exports a symbol for same-package consumers.
use { vis_internal } from "./sub/visibility_reexporter.wado";

test "internal use re-export resolves within the package" {
    assert vis_internal() == 2;
}
