// Test for-of with Array of tuples (concrete types)
use { println, Stdout } from "core:cli";

fn run() with Stdout {
    let items: Array<[String, i32]> = [["a", 1], ["b", 2]];
    for let pair of items {
        println(pair.0);
    }
    println("done");
}

__DATA__
{"stdout": "a\nb\ndone\n"}
