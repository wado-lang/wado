// Basic for-of loop with integers

use { println, Stdout } from "core:cli";

fn run() with Stdout {
    let numbers: Array<i32> = [1, 2, 3, 4, 5];
    let mut sum = 0;
    for let n of numbers {
        sum = sum + n;
    }
    println(`sum = {sum}`);
}

__DATA__
{"stdout": "sum = 15\n"}
