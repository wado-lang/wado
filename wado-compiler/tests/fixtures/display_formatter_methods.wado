// Test: Formatter pub fields (width, precision, indent, sign_plus)
use { println, Stdout } from "core:cli";

export fn run() with Stdout {
    let mut buf = String::with_capacity(64);
    let f = Formatter::new(&mut buf);

    // Default spec values via direct field access
    println(`width: {f.width}`);
    println(`precision: {f.precision}`);
    println(`indent: {f.indent}`);
    println(`sign_plus: {f.sign_plus}`);
}

__DATA__
{"stdout": "width: -1\nprecision: -1\nindent: 0\nsign_plus: false\n"}
