// Test C-style for loop with number variables

use {println, Stdout} from "core:cli";

fn run() with Stdout {
    for (let mut i = 0; i < 3; i = i + 1) {
        println("loop");
    }
    println("done");
}
