//! A facade re-exports a `pub` item under its own name, so consumers reach it
//! without naming the file it lives in.

use { facade_value } from "./sub/visibility_facade.wado";

test "pub use re-exports through a facade, under an alias" {
    assert facade_value() == 42;
}
