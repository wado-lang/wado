use { println, Stdout } from "core:cli";

#[param]
global mut COUNT: i32 = 0;

export fn run() with Stdout {
    println(`COUNT=${COUNT}`);
}

__DATA__
{"compile_error": "#[param] cannot be applied to a mutable global"}
