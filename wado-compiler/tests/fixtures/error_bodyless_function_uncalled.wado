// The declaration is the error, so it is reported with nothing calling it.
// A check at the call site would say nothing about a module that only
// declares.

fn missing() -> i32;

export fn run() {}

__DATA__
{"compile_error": "function 'missing' has no body"}
