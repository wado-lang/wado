// For-of loop over empty array
// Should not execute loop body

use { println, Stdout } from "core:cli";

fn run() with Stdout {
    let empty: Array<i32> = [];
    for let item of empty {
        println(`should not print: {item}`);
    }
    println("done");
}

__DATA__
{"stdout": "done\n"}
