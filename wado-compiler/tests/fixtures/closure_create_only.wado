// Test closure creation (without calling)
use { println, Stdout } from "core:cli";

export fn run() with Stdout {
    let a = 10;
    let f = |x: i32| x + a;
    // f is created but not called
    println("closure created");
}

__DATA__
{"stdout": "closure created\n"}
