// When the aggregate is still live after the materialization, the field copy must be kept.

use { println, Stdout } from "core:cli";

struct Cols { a: List<i32>, b: List<i32> }
struct Out { x: List<i32>, y: List<i32> }

fn split_and_peek(c: Cols) -> i32 {
    let mut o = Out { x: c.a, y: c.b };
    o.x.push(999);
    return c.a.len() + o.x.len();
}

fn make(n: i32) -> Cols {
    let mut a: List<i32> = [];
    let mut b: List<i32> = [];
    let mut i = 0;
    loop {
        if i >= n { break; }
        a.push(i);
        b.push(i);
        i = i + 1;
    }
    return Cols { a, b };
}

export fn run() with Stdout {
    println(`${split_and_peek(make(3))}`);
}

__DATA__
{
    "stdout": "7\n",
    "wir_expect:O0": ["$value_copy$core:prelude/list.wado/List<i32>"]
}
