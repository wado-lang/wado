// An export is held to the same rule: a parameter always carries a type, and
// `()` carries none.
export fn take(u: ()) -> i32 {
    return 1;
}

export fn run() {
}

__DATA__
{"compile_error": "export function `take`: `()` has no Component Model representation here — it stands only where a type may be absent: a function result, a `Result` arm or a case payload"}
