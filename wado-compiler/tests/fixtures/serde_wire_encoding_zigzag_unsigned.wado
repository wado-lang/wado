// ZigZag moves a sign into the low bit, so an unsigned field has none to move.

struct Sample {
    #[wire(encoding = "zigzag")] count: u32,
}

export fn run() {}

__DATA__
{"compile_error": "`#[wire(encoding = \"zigzag\")]` needs a signed integer, `i32` or `i64`, and `count` holds `u32`", "compile_error_codes": ["WIRE_ENCODING"]}
