use {println, Stdout} from "core:cli";

// Error case: non-unit if expression without else
export fn run() with Stdout {
    let x = if true { 42 };
    println(`${x}`);
}

__DATA__
{"compile_error": "type mismatch"}
