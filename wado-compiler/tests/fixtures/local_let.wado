// Test local variables with let
// Note: println adds newline for literals but not for variables,
// so we include the newline in the string

use {println, Stdout} from "core:cli";

fn main() with Stdout {
    let msg = "Hello from let!\n";
    println(msg);
}
