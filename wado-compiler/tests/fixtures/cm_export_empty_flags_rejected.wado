// A Component Model `flags` lists at least one member, so an empty one has no
// type to lower to.
pub flags Nothing {}

export fn take(n: Nothing) -> u32 {
    return 1;
}

export fn run() {
}

__DATA__
{"compile_error": "export function `take`: `Nothing` declares no case, and an empty enum, flags or variant has no Component Model representation — add at least one"}
