// A module without the prelude imports what it uses from `core:prelude`.

#![no_prelude]

use { String, List, Option, Result, Stream, Future } from "core:prelude";

test {
    let xs: List<i32> = [1, 2];
    assert xs.len() == 2;
}
