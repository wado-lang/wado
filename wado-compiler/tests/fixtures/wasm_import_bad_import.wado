// Only `env.memory` is routed through to an imported wasm asset; any
// other import (here `env.table`) is a compile error with a pointed
// message listing the offending import.

use _ from "./sub/wasm_import_bad_import.wat" with { type: "wat" };

export fn run() {}

__DATA__
{
    "compile_error": "only the `env.memory` import is supported"
}
