// A newline does not separate statements — there is no automatic semicolon
// insertion — so two statements always need a `;` between them.

export fn run() {
    let x = 1
    let y = 2;
}

__DATA__
{"compile_error": "expected `;` or `}`"}
