// `resume` begins `resume value`, so a binding spelled `resume` could be
// declared but never read. Every declaration of one is refused; a field or
// method, reached through `.`, may still take the name.
struct Holder { resume: i32 }

fn take(resume: i32) -> i32 { return 0; }

export fn run() {
    let resume = 42;
    let h = Holder { resume: 1 };
    let r = h.resume;
}

__DATA__
{
  "compile_error": "`resume` cannot be a name: it is the keyword that begins `resume value`",
  "compile_error_count": 2,
  "compile_error_codes": ["INVALID_SYNTAX"]
}
