// if-let chain scope test: binding is not visible outside the if block
use { println, Stdout } from "core:cli";

export fn run() with Stdout {
    if let x = 10 && x > 5 {
        println(`${x}`);
    }
    println(`${x}`);
}

__DATA__
{
    "compile_error": "not in scope"
}
