// Test importing from a local .wado module

use {println, Stdout} from "core:cli";
use {add} from "./user_function_call.wado";

fn run() with Stdout {
    let result = add(40, 2);

    if result == 42 {
        println("success");
    } else {
        println("failure");
    }
}
