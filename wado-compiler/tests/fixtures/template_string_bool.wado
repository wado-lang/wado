// Test template string interpolation with bool values

use {println, Stdout} from "core:cli";

fn run() with Stdout {
    let t = true;
    let f = false;
    println(`true: {t}`);
    println(`false: {f}`);
}
