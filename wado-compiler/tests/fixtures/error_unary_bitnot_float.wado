// `~` complements an integer bit pattern. A float has no such lowering, so the
// operator is rejected rather than reinterpreting the mantissa.

export fn run() {
    let _bits = ~1.5;
}

__DATA__
{"compile_error": "operator `~` cannot be applied to type `f64`"}
