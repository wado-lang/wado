// Test template string interpolation with char values

use {println, Stdout} from "core:cli";

fn run() with Stdout {
    let a = 'A';
    let z = 'Z';
    println(`char A: {a}`);
    println(`char Z: {z}`);
}
