// One numbered field says the struct is meant for a numbered wire, so a
// sibling without a number is a mistake in the declaration rather than a
// field some format will quietly drop.

struct Point {
    #[wire(number = 1)] x: i32,
    y: i32,
}

export fn run() {}

__DATA__
{"compile_error": "`y` carries no `#[wire(number = …)]` and `x` does: a struct numbers every field or none", "compile_error_codes": ["WIRE_NUMBER"]}
