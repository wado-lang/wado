// A name that was taken out says so in the same sentence: the version is prose
// the author writes, not an argument the compiler reads.

#[unavailable("removed in 0.5.0; use `Option::ok_or`")]
fn into_result();

export fn run() {
    into_result();
}

__DATA__
{"compile_error": "`into_result` is unavailable: removed in 0.5.0; use `Option::ok_or`"}
