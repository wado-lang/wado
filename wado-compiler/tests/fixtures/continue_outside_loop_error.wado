// `continue` has no target either without an enclosing loop.

export fn run() {
    let c = builtin::black_box(true);
    if c {
        continue;
    }
}

__DATA__
{"compile_error": "`continue` outside of a loop"}
