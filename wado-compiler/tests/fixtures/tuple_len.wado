export fn run() {
    let t = [1, "hello", true];
    assert t.len() == 3;

    let pair = [42, 99];
    assert pair.len() == 2;

    let single = [42];
    assert single.len() == 1;

    let empty = [];
    assert empty.len() == 0;
}

__DATA__
{"stdout": ""}
