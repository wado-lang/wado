// Test error: an if expression whose both branches are a bare `null` has
// no way to determine its type. Reported instead of letting an unresolved
// `Option<UNKNOWN>` reach codegen.

export fn run() {
    let x = if true { null } else { null };
}

__DATA__
{"compile_error": "cannot infer the type of this if expression"}
