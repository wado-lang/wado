// Two handles compare only when one type extends the other.
use { Element, Event } from "web:dom";

fn same(el: Element, ev: Event) -> bool {
    return el == ev;
}

export fn run() {}

__DATA__
{"compile_error": "operator `==` cannot be applied to types `Element` and `Event`"}
