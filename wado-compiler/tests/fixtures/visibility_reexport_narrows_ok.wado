//! Re-exporting at or below the item's own rung is fine.
use { Open } from "./sub/vis_reexport_facade.wado";

struct T {}

impl Open for T {
    fn o(&self) -> i32 { return 7 }
}

test "a pub item re-exported pub stays reachable" {
    assert T {}.o() == 7;
}

__DATA__
{ "test": {} }
