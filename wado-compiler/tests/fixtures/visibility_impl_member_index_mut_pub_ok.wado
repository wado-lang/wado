//! A `pub` `&mut self` method is reachable through an indexed place.
use { make_items } from "./sub/impl_member_lib.wado";

test "pub mutating method through an index" {
    let mut items = make_items();
    assert items[0].bump_open() == 1;
}
