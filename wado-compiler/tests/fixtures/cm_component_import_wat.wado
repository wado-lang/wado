use { Math } from "./sub/wasm_import_cm_math.wat" with { type: "wat" };

test "a component asset written as wat is composed in" {
    assert Math::add(2, 40) == 42;
    assert Math::add(0, 0) == 0;
}
