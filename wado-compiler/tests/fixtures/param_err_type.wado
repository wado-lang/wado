use { println, Stdout } from "core:cli";

#[param]
global ITEMS: List<i32> = [1, 2, 3];

export fn run() with Stdout {
    println(`len=${ITEMS.len()}`);
}

__DATA__
{"compile_error": "only built-in types are supported in v1"}
