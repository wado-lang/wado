// A `let` or `for` binding reads a bare name the way `match` does: a case of
// the scrutinee's type is a case pattern, so one that can fail is rejected.

variant Two { A(i32), B }

test {
    let o: Option<i32> = Some(7);
    let [None, x] = [o, 1];
    let [B, y] = [Two::A(1), 2];
    for let A(z) of [Two::A(1), Two::B] {}
}

__DATA__
{"compile_error": ":8:5: analysis error: invalid pattern: refutable pattern in `let` binding: `None` may not match; use `let ... else` or `if let` instead; 9:5: invalid pattern: refutable pattern in `let` binding: `B` may not match; use `let ... else` or `if let` instead; 10:5: invalid pattern: refutable pattern in `for` binding: `A` may not match; match on the element in the loop body instead"}
