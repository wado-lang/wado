// Orphan rule violation: impl foreign trait for foreign generic type → FORBIDDEN
// Eq is foreign, List<i32> head type is List which is foreign (core:prelude)
// Even though i32 is inside, the outermost type constructor (List) is foreign → FORBIDDEN

impl Eq for List<i32> {
    fn eq(&self, other: &Self) -> bool {
        return true;
    }
}

export fn run() {}

__DATA__
{"compile_error": "orphan rule"}
