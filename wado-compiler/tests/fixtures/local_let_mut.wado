// Test local variables with let mut and reassignment

use {println, Stdout} from "core:cli";

fn run() with Stdout {
    let mut msg = "First message";
    println(msg);
    msg = "Second message";
    println(msg);
}
