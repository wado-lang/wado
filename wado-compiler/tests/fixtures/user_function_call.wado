// Test user-defined function call

use {println, Stdout} from "core:cli";

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn run() with Stdout {
    let result = add(40, 2);

    if result == 42 {
        println("success");
    } else {
        println("failure");
    }
}
