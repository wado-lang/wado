// Gap: a subscript's receiver renders nothing. The index and the element are
// captured, so the sequence they read from should be too.

export fn run() {
    let list: List<i32> = [10, 20];
    let i = 1;
    assert list[i] == 99;
}

__DATA__
{
    "trapped": true,
    "stderr_contains": ["list[i]: 20", "list: ["]
}
