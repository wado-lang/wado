// A variant payload names a type this module never imported. It goes through
// no `let` check, so the declared-type invariant (`unresolved_types`) is what
// keeps the class a diagnostic rather than an assert.

use { from_bytes } from "core:json";

struct Rec {
    a: String,
}

variant Wrapped {
    Payload(Result<Rec, DeserializeError>),
    Empty,
}

export fn run() {
}

__DATA__
{"compile_error": "unknown type 'DeserializeError'"}
