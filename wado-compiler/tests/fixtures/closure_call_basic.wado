// Test basic closure call
use { println, Stdout } from "core:cli";

export fn run() with Stdout {
    let add_one = |x: i32| x + 1;
    let result = add_one(5);
    println(`{result}`);
}

__DATA__
{"stdout": "6\n"}
