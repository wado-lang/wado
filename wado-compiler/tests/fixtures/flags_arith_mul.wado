pub flags Perms {
    Read,
    Write,
}

export fn run() {
    let r = Perms::Read;
    let bad = r * r;
    println(`{bad as u32}`);
}

__DATA__
{"compile_error": "operator `*` cannot be applied to type"}
