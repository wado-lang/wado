#![TODO]
// Module-level #![TODO] with a compile error.
// The module is TODO, so compile errors are expected and treated as pass.

fn helper() -> NonExistentType {
    return 42;
}

test "uses broken helper" {
    let x = helper();
    assert x == 42;
}
