test "underscore in let annotation is not yet supported" {
    let o: Option<_> = Option::Some(42);
    assert o == Option::Some(42);
}

__DATA__
{"compile_error": "type inference placeholder `_` in a `let` annotation is not yet supported"}
