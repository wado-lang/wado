//! Test float formatting in string templates using builtin ryu
//!
//! This tests that float literals in template strings are formatted
//! deterministically using the ryu algorithm.

use {println} from "core:cli";

pub fn run() -> Result<(), ()> {
    // Test basic float formatting (single part - no concatenation needed)
    println(`{3.14159}`);
}
