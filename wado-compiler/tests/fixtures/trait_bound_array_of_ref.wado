// Test: trait bounds with Array of references — Array<&T> where T: Eq
use { println, Stdout } from "core:cli";

export fn run() with Stdout {
    let a = 10;
    let b = 20;
    let arr: Array<&i32> = [&a, &b];
    println(`{*arr[0]}`);
    println(`{*arr[1]}`);
}

__DATA__
{"stdout": "10\n20\n"}
