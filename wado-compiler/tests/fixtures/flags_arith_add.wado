pub flags Perms {
    Read,
    Write,
}

export fn run() {
    let r = Perms::Read;
    let w = Perms::Write;
    let bad = r + w;
    println(`{bad as u32}`);
}

__DATA__
{"compile_error": "operator `+` cannot be applied to type"}
