// The type half of `ns_trait_static.wado`: a trait static on a type the
// importing module reaches only through its namespace.

pub struct P {}

pub trait Mk {
    fn make(n: i32 = 7) -> i32;
}

impl Mk for P {
    fn make(n: i32) -> i32 {
        return n;
    }
}
