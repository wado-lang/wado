// Helper module: exposes a struct whose field is a closure type.
// Imported in `closure_cm_boundary_imported_struct_error.wado` to verify
// that `type_contains_closure` recurses through cross-module struct
// field types when the struct crosses the CM boundary.

pub struct ImportedHolder {
    pub f: fn(i32) -> i32,
}
