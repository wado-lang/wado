// The foreign blanket whose bound holds at the newtype itself.
use { Limit, Describe } from "./trait_rank2_lib.wado";

impl<T: Limit> Describe for T {
    fn describe(&self) -> String {
        return `limit:${T::limit()}`;
    }
}
