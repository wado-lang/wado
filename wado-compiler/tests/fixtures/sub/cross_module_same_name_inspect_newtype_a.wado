// `widen` is what makes this `Span` reach a derived `Inspect`, so the bare-name
// entry the lookup fell through to exists at all.
pub type Span = u64;

pub struct Anchor {
    pub at: i64,
}

pub fn widen(a: Anchor) -> Span {
    return a.at as Span;
}
