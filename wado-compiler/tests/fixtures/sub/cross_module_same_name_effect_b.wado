// Module B: declares a DIFFERENT `pub effect Logger` (same bare name,
// different method set) plus a function that requires it.
pub interface Logger {
    fn warn(code: i32);
}

pub fn raise(code: i32) with Logger {
    let _ = code;
}
