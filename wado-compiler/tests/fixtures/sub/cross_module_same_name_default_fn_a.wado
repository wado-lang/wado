// Module A: a parameter default calls A's own private `tag`.
// The entry module declares a `tag` of its own, so a default resolved
// from the *caller's* scope reaches the wrong declaration.

fn tag() -> i32 {
    return 1;
}

pub fn labelled(v: i32 = tag()) -> i32 {
    return v;
}
