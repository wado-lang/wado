// The trait half of `default_arg_trait_static.wado`: the declaration carrying
// the default lives in another module than the impl that implements it.

pub trait Tag {
    fn tagged(n: i32 = 3) -> i32;
}
