// A generic type reached only through a namespace import, so the consumer's
// `impl From<geo::Pair<T>>` writes its argument as `ast::Type::NamespacedGeneric`.

pub struct Pair<T> {
    pub a: T,
    pub b: T,
}
