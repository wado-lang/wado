// Module B: declares a DIFFERENT `pub interface Logger` (same bare name,
// different operations). Imported by the parent as `Logger as LoggerB`.
pub interface Logger {
    fn warn(code: i32);
}

pub fn raise(code: i32) with Logger {
    let _ = code;
}
