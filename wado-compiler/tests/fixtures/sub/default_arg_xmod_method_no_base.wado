// Helper for `default_arg_xmod_method.wado`: a caller that spells no `BASE`,
// so the method default has nothing at this end to resolve to by accident.

use { Box3 } from "./default_arg_xmod_method_lib.wado";

pub fn without_base() -> i32 {
    return Box3::new(1).plus();
}
