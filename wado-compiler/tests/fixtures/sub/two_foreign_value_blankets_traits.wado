// The traits both blanket modules key on, declared where neither owns them.
pub trait Limit {
    fn limit() -> i32;
}

pub trait Mark {
    fn mark() -> i32;
}

pub trait Describe {
    fn describe(&self) -> String;
}
