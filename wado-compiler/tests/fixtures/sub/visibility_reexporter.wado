//! Re-exports a package-internal symbol via `internal use` (same-package
//! re-export). Consumers in the same package can import it through here.
internal use { vis_internal } from "./visibility_lib.wado";
