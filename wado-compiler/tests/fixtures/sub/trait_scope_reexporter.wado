// Pulls the impl module in so the caller reaches it without naming it.
use { Whisperer } from "./trait_scope_impl_only.wado";

pub fn make_whisperer(id: i32) -> Whisperer {
    return Whisperer { id: id };
}
