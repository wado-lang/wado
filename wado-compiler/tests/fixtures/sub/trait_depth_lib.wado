// The traits and the base type for `trait_rank2_beats_locality`, declared
// where the calling module does not own them.
pub trait Limit {
    fn limit() -> i32;
}

pub trait Base {
    fn base() -> i32;
}

pub trait Describe {
    fn describe(&self) -> String;
}

pub struct Inner {
    pub x: i32,
}
