// Module B: declares a DIFFERENT `pub resource Counter` (same bare
// name, different method set). The two Counters must stay distinct.
pub resource Counter {
    #[cm("counter-reset")]
    fn reset(&mut self);
}

pub fn tick_b(c: &Counter) {
    let _ = c;
}
