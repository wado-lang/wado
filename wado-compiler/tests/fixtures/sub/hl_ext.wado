// Provider source for `provider_surface.wado`: compiled on-demand into a
// component exporting `test:hlc/highlight`, satisfying the dependency's import.
export fn highlight(code: String, lang: String) -> String {
    return `${lang}:${code}`;
}
