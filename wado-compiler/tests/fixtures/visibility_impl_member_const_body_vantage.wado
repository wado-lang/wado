//! A constant's body is judged where it is written, not where it is read.
use { Item } from "./sub/impl_member_lib.wado";

test "a pub constant may reach its own file's private members" {
    assert Item::DERIVED == 42;
}
