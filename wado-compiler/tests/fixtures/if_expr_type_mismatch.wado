use {println, Stdout} from "core:cli";

// Error case: branch types don't match
fn run() with Stdout {
    let x = if true { 42 } else { "hello" };
    println(`{x}`);
}

__DATA__
{"compile_error": "type mismatch"}
