// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/iterator_fold.wado
// Generated by: make update-golden-fixtures

fn run() with Stdout {
    let arr: Array<i32> = move [1, 2, 3, 4, 5];
    let sum: i32 = __inline_ArrayIter_i32___fold_i32__0: {
        let self: &mut ArrayIter<i32> = &mut __inline_Array_i32___iter_1: {
            break __inline_Array_i32___iter_1: ArrayIter<i32> { repr: arr.repr, used: arr.used, index: 0 };
        };
        let f: fn(i32, i32) -> i32 = |acc: i32, x: i32| (acc + x);
        let mut acc: i32 = 0;
        loop {
            if Some(item) = "ArrayIter<i32>^Iterator::next"(self) {
                acc = f(acc, item);
            } else {
                break;
            }
        }
        break __inline_ArrayIter_i32___fold_i32__0: acc;
    };
    core::cli::println(__inline_string_concat_2: {
        let a: String = move "sum: ";
        let b: String = move "i32::to_string"(sum);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_2: result;
    });
    let product: i32 = __inline_ArrayIter_i32___fold_i32__3: {
        let self: &mut ArrayIter<i32> = &mut __inline_Array_i32___iter_4: {
            break __inline_Array_i32___iter_4: ArrayIter<i32> { repr: arr.repr, used: arr.used, index: 0 };
        };
        let f: fn(i32, i32) -> i32 = |acc: i32, x: i32| (acc * x);
        let mut acc: i32 = 1;
        loop {
            if Some(item) = "ArrayIter<i32>^Iterator::next"(self) {
                acc = f(acc, item);
            } else {
                break;
            }
        }
        break __inline_ArrayIter_i32___fold_i32__3: acc;
    };
    core::cli::println(__inline_string_concat_5: {
        let a: String = move "product: ";
        let b: String = move "i32::to_string"(product);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_5: result;
    });
    let count: i32 = __inline_ArrayIter_i32___fold_i32__6: {
        let self: &mut ArrayIter<i32> = &mut __inline_Array_i32___iter_7: {
            break __inline_Array_i32___iter_7: ArrayIter<i32> { repr: arr.repr, used: arr.used, index: 0 };
        };
        let f: fn(i32, i32) -> i32 = |acc: i32, _x: i32| (acc + 1);
        let mut acc: i32 = 0;
        loop {
            if Some(item) = "ArrayIter<i32>^Iterator::next"(self) {
                acc = f(acc, item);
            } else {
                break;
            }
        }
        break __inline_ArrayIter_i32___fold_i32__6: acc;
    };
    core::cli::println(__inline_string_concat_8: {
        let a: String = move "count: ";
        let b: String = move "i32::to_string"(count);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_8: result;
    });
    let arr2: Array<i32> = move [3, 1, 4, 1, 5, 9, 2, 6];
    let max: i32 = __inline_ArrayIter_i32___fold_i32__9: {
        let self: &mut ArrayIter<i32> = &mut __inline_Array_i32___iter_10: {
            break __inline_Array_i32___iter_10: ArrayIter<i32> { repr: arr2.repr, used: arr2.used, index: 0 };
        };
        let f: fn(i32, i32) -> i32 = |acc: i32, x: i32| if (x > acc) {
            x;
        } else {
            acc;
        };
        let mut acc: i32 = 0;
        loop {
            if Some(item) = "ArrayIter<i32>^Iterator::next"(self) {
                acc = f(acc, item);
            } else {
                break;
            }
        }
        break __inline_ArrayIter_i32___fold_i32__9: acc;
    };
    core::cli::println(__inline_string_concat_11: {
        let a: String = move "max: ";
        let b: String = move "i32::to_string"(max);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_11: result;
    });
}

fn "ArrayIter<i32>^Iterator::next"(self: &mut ArrayIter<i32>) -> Option<i32> {
    if (self.index >= self.used) {
        return null;
    }
    let item: i32 = "array_get<i32>"(self.repr, self.index);
    self.index = (self.index + 1);
    return Option::Some(item);
}

fn "array_get<i32>"(arr: builtin::array<i32>, idx: i32) -> i32;

