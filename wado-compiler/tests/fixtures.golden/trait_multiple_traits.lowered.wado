// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/trait_multiple_traits.wado
// Generated by: make update-golden-fixtures

struct Person {
    name: String,
    age: i32,
    city: String,
}

fn run() with Stdout {
    let p: Person = move Person { name: "Alice", age: 30, city: "Tokyo" };
    core::cli::println(__inline_string_concat_1: {
        let a: String = move "Name: ";
        let b: String = __inline_Person_Named__name_0: {
            break __inline_Person_Named__name_0: p.name;
        };
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_1: result;
    });
    core::cli::println(__inline_string_concat_3: {
        let a: String = move "Age: ";
        let b: String = move "i32::to_string"(__inline_Person_Aged__age_2: {
            break __inline_Person_Aged__age_2: p.age;
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_3: result;
    });
    core::cli::println(__inline_string_concat_5: {
        let a: String = move "Location: ";
        let b: String = __inline_Person_Located__location_4: {
            break __inline_Person_Located__location_4: p.city;
        };
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_5: result;
    });
}

