// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/static_method_generic.wado
// Generated by: make update-golden-fixtures

fn run() with Stdout {
    let mut arr: Array<i32> = move "Array<i32>::with_capacity"(10);
    core::cli::println(__inline_string_concat_1: {
        let a: String = move "initial len: ";
        let b: String = move "i32::to_string"(__inline_Array_i32___len_0: {
            let self: &Array<i32> = &arr;
            break __inline_Array_i32___len_0: self.used;
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_1: result;
    });
    core::cli::println(__inline_string_concat_3: {
        let a: String = move "initial capacity: ";
        let b: String = move "i32::to_string"(__inline_Array_i32___capacity_2: {
            let self: &Array<i32> = &arr;
            break __inline_Array_i32___capacity_2: core::builtin::array_len(self.repr);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_3: result;
    });
    __inline_Array_i32___append_4: {
        let self: &mut Array<i32> = &mut arr;
        let value: i32 = 1;
        let used: i32 = self.used;
        let capacity: i32 = core::builtin::array_len(self.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
            core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
            self.repr = new_repr;
        }
        core::builtin::array_set::<i32>(self.repr, used, value);
        self.used = (used + 1);
    };
    __inline_Array_i32___append_5: {
        let self: &mut Array<i32> = &mut arr;
        let value: i32 = 2;
        let used: i32 = self.used;
        let capacity: i32 = core::builtin::array_len(self.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
            core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
            self.repr = new_repr;
        }
        core::builtin::array_set::<i32>(self.repr, used, value);
        self.used = (used + 1);
    };
    __inline_Array_i32___append_6: {
        let self: &mut Array<i32> = &mut arr;
        let value: i32 = 3;
        let used: i32 = self.used;
        let capacity: i32 = core::builtin::array_len(self.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
            core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
            self.repr = new_repr;
        }
        core::builtin::array_set::<i32>(self.repr, used, value);
        self.used = (used + 1);
    };
    core::cli::println(__inline_string_concat_8: {
        let a: String = move "after append len: ";
        let b: String = move "i32::to_string"(__inline_Array_i32___len_7: {
            let self: &Array<i32> = &arr;
            break __inline_Array_i32___len_7: self.used;
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_8: result;
    });
    core::cli::println(__inline_string_concat_10: {
        let a: String = move "after append capacity: ";
        let b: String = move "i32::to_string"(__inline_Array_i32___capacity_9: {
            let self: &Array<i32> = &arr;
            break __inline_Array_i32___capacity_9: core::builtin::array_len(self.repr);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_10: result;
    });
    core::cli::println(__inline_string_concat_12: {
        let a: String = move "arr[0]: ";
        let b: String = move "i32::to_string"(__inline_Array_i32__IndexValue__index_value_11: {
            let self: &Array<i32> = &arr;
            let index: i32 = 0;
            break __inline_Array_i32__IndexValue__index_value_11: core::builtin::array_get::<i32>(self.repr, index);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_12: result;
    });
    core::cli::println(__inline_string_concat_14: {
        let a: String = move "arr[1]: ";
        let b: String = move "i32::to_string"(__inline_Array_i32__IndexValue__index_value_13: {
            let self: &Array<i32> = &arr;
            let index: i32 = 1;
            break __inline_Array_i32__IndexValue__index_value_13: core::builtin::array_get::<i32>(self.repr, index);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_14: result;
    });
    core::cli::println(__inline_string_concat_16: {
        let a: String = move "arr[2]: ";
        let b: String = move "i32::to_string"(__inline_Array_i32__IndexValue__index_value_15: {
            let self: &Array<i32> = &arr;
            let index: i32 = 2;
            break __inline_Array_i32__IndexValue__index_value_15: core::builtin::array_get::<i32>(self.repr, index);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_16: result;
    });
}

pub fn "Array<i32>::with_capacity"(capacity: i32) -> Array<i32> {
    return Array<i32> { repr: core::builtin::array_new::<i32>(capacity), used: 0 };
}

