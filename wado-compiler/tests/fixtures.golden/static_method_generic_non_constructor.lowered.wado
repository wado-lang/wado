// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/static_method_generic_non_constructor.wado
// Generated by: make update-golden-fixtures

struct "Counter<i32>" {
    value: i32,
}

fn run() with Stdout {
    let z: i32 = "Counter<i32>::zero"();
    core::cli::println(__inline_string_concat_0: {
        let a: String = move "zero: ";
        let b: String = move "i32::to_string"(z);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_0: result;
    });
    let d: i32 = "Counter<i32>::default_value"(100);
    core::cli::println(__inline_string_concat_1: {
        let a: String = move "default: ";
        let b: String = move "i32::to_string"(d);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_1: result;
    });
    let c: Counter<i32> = move "Counter<i32>::new"(42);
    core::cli::println(__inline_string_concat_3: {
        let a: String = move "counter value: ";
        let b: String = move "i32::to_string"(__inline_Counter_i32___get_2: {
            break __inline_Counter_i32___get_2: c.value;
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_3: result;
    });
}

pub fn "Counter<i32>::new"(value: i32) -> Counter<i32> {
    return Counter<i32> { value: value };
}

pub fn "Counter<i32>::default_value"(v: i32) -> i32 {
    return v;
}

pub fn "Counter<i32>::zero"() -> i32 {
    return 0;
}

