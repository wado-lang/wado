// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/index_value_generic.wado
// Generated by: make update-golden-fixtures

struct "Triple<i64>" {
    first: i64,
    second: i64,
    third: i64,
}

struct "Triple<i32>" {
    first: i32,
    second: i32,
    third: i32,
}

fn run() with Stdout {
    let nums: Triple<i32> = move Triple<i32> { first: 10, second: 20, third: 30 };
    let n0: i32 = "Triple<i32>^IndexValue::index_value"(&nums, 0);
    let n1: i32 = "Triple<i32>^IndexValue::index_value"(&nums, 1);
    let n2: i32 = "Triple<i32>^IndexValue::index_value"(&nums, 2);
    core::cli::println(__inline_string_concat_4: {
        let a: String = __inline_string_concat_3: {
            let a: String = __inline_string_concat_2: {
                let a: String = __inline_string_concat_1: {
                    let a: String = __inline_string_concat_0: {
                        let a: String = move "nums: ";
                        let b: String = move "i32::to_string"(n0);
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_0: result;
                    };
                    let b: String = move ", ";
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_1: result;
                };
                let b: String = move "i32::to_string"(n1);
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_2: result;
            };
            let b: String = move ", ";
            let len_a: i32 = "String::len"(&a);
            let len_b: i32 = "String::len"(&b);
            let total: i32 = (len_a + len_b);
            let result: String = move core::builtin::string_new(total);
            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                let byte: i32 = "String::get"(&a, i);
                "String::set"(&mut result, i, byte);
            }
            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                let byte: i32 = "String::get"(&b, i);
                "String::set"(&mut result, (len_a + i), byte);
            }
            break __inline_string_concat_3: result;
        };
        let b: String = move "i32::to_string"(n2);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_4: result;
    });
    let longs: Triple<i64> = move Triple<i64> { first: 100 as i64, second: 200 as i64, third: 300 as i64 };
    let l0: i64 = "Triple<i64>^IndexValue::index_value"(&longs, 0);
    let l1: i64 = "Triple<i64>^IndexValue::index_value"(&longs, 1);
    core::cli::println(__inline_string_concat_7: {
        let a: String = __inline_string_concat_6: {
            let a: String = __inline_string_concat_5: {
                let a: String = move "longs: ";
                let b: String = move "i64::to_string"(l0);
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_5: result;
            };
            let b: String = move ", ";
            let len_a: i32 = "String::len"(&a);
            let len_b: i32 = "String::len"(&b);
            let total: i32 = (len_a + len_b);
            let result: String = move core::builtin::string_new(total);
            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                let byte: i32 = "String::get"(&a, i);
                "String::set"(&mut result, i, byte);
            }
            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                let byte: i32 = "String::get"(&b, i);
                "String::set"(&mut result, (len_a + i), byte);
            }
            break __inline_string_concat_6: result;
        };
        let b: String = move "i64::to_string"(l1);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_7: result;
    });
    let sum: i32 = (("Triple<i32>^IndexValue::index_value"(&nums, 0) + "Triple<i32>^IndexValue::index_value"(&nums, 1)) + "Triple<i32>^IndexValue::index_value"(&nums, 2));
    core::cli::println(__inline_string_concat_8: {
        let a: String = move "sum: ";
        let b: String = move "i32::to_string"(sum);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_8: result;
    });
}

fn "Triple<i64>^IndexValue::index_value"(self: &Triple<i64>, index: i32) -> i64 {
    if (index == 0) {
        return self.first;
    } else {
        if (index == 1) {
            return self.second;
        } else {
            return self.third;
        }
    }
}

fn "Triple<i32>^IndexValue::index_value"(self: &Triple<i32>, index: i32) -> i32 {
    if (index == 0) {
        return self.first;
    } else {
        if (index == 1) {
            return self.second;
        } else {
            return self.third;
        }
    }
}

