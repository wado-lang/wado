// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/precedence_unary.wado
// Generated by: make update-golden-fixtures

fn run() {
    let a: i32 = (-3 + 5);
    assert a == 2;  // (a == 2)
    let b: i32 = (-2 * 3);
    assert b == -;  // (b == -6)
    let c: i32 = (10 - -3);
    assert c == 13;  // (c == 13)
    let d: bool = (!true || true);
    assert d == true;  // (d == true)
    let e: bool = (!false && false);
    assert e == false;  // (e == false)
    let f: i32 = (~0 + 2);
    assert f == 1;  // (f == 1)
    let g: i32 = (~1 & 3);
    assert g == 2;  // (g == 2)
    let h: i32 = --5;
    assert h == 5;  // (h == 5)
    let i: bool = !!true;
    assert i == true;  // (i == true)
    let j: bool = !!false;
    assert j == false;  // (j == false)
    let k: i32 = ~~0;
    assert k == 0;  // (k == 0)
    let n: bool = (-5 < 0);
    assert n == true;  // (n == true)
    let o: bool = (!true == false);
    assert o == true;  // (o == true)
    let p: i32 = (-2 + (3 * -4));
    assert p == -;  // (p == -14)
}

__DATA__
{}

