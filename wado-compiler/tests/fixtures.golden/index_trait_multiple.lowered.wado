// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/index_trait_multiple.wado
// Generated by: make update-golden-fixtures

struct Point {
    x: i32,
    y: i32,
}

struct PointHolder {
    point: Point,
}

fn run() with Stdout {
    let mut h1: PointHolder = move PointHolder { point: Point { x: 0, y: 0 } };
    let mut h2: PointHolder = move PointHolder { point: Point { x: 10, y: 10 } };
    __inline_Point__move_by_0: {
        let self: &mut Point = __inline_PointHolder_IndexMut__index_mut_1: {
            let self: &mut PointHolder = &mut h1;
            let _idx: i32 = 0;
            break __inline_PointHolder_IndexMut__index_mut_1: &mut self.point;
        };
        let dx: i32 = 5;
        let dy: i32 = 5;
        self.x = (self.x + dx);
        self.y = (self.y + dy);
    };
    __inline_Point__move_by_2: {
        let self: &mut Point = __inline_PointHolder_IndexMut__index_mut_3: {
            let self: &mut PointHolder = &mut h2;
            let _idx: i32 = 0;
            break __inline_PointHolder_IndexMut__index_mut_3: &mut self.point;
        };
        let dx: i32 = -3;
        let dy: i32 = -3;
        self.x = (self.x + dx);
        self.y = (self.y + dy);
    };
    let p1: Point = *__inline_PointHolder_Index__index_4: {
        let self: &PointHolder = &h1;
        let _idx: i32 = 0;
        break __inline_PointHolder_Index__index_4: &self.point;
    };
    let p2: Point = *__inline_PointHolder_Index__index_5: {
        let self: &PointHolder = &h2;
        let _idx: i32 = 0;
        break __inline_PointHolder_Index__index_5: &self.point;
    };
    core::cli::println(__inline_string_concat_9: {
        let a: String = __inline_string_concat_8: {
            let a: String = __inline_string_concat_7: {
                let a: String = __inline_string_concat_6: {
                    let a: String = move "h1: (";
                    let b: String = move "i32::to_string"(p1.x);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    let mut i: i32 = 0;
                    for ; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    let mut i: i32 = 0;
                    for ; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_6: result;
                };
                let b: String = move ", ";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                let mut i: i32 = 0;
                for ; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                let mut i: i32 = 0;
                for ; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_7: result;
            };
            let b: String = move "i32::to_string"(p1.y);
            let len_a: i32 = "String::len"(&a);
            let len_b: i32 = "String::len"(&b);
            let total: i32 = (len_a + len_b);
            let result: String = move core::builtin::string_new(total);
            let mut i: i32 = 0;
            for ; (i < len_a); i = (i + 1) {
                let byte: i32 = "String::get"(&a, i);
                "String::set"(&mut result, i, byte);
            }
            let mut i: i32 = 0;
            for ; (i < len_b); i = (i + 1) {
                let byte: i32 = "String::get"(&b, i);
                "String::set"(&mut result, (len_a + i), byte);
            }
            break __inline_string_concat_8: result;
        };
        let b: String = move ")";
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_9: result;
    });
    core::cli::println(__inline_string_concat_13: {
        let a: String = __inline_string_concat_12: {
            let a: String = __inline_string_concat_11: {
                let a: String = __inline_string_concat_10: {
                    let a: String = move "h2: (";
                    let b: String = move "i32::to_string"(p2.x);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    let mut i: i32 = 0;
                    for ; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    let mut i: i32 = 0;
                    for ; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_10: result;
                };
                let b: String = move ", ";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                let mut i: i32 = 0;
                for ; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                let mut i: i32 = 0;
                for ; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_11: result;
            };
            let b: String = move "i32::to_string"(p2.y);
            let len_a: i32 = "String::len"(&a);
            let len_b: i32 = "String::len"(&b);
            let total: i32 = (len_a + len_b);
            let result: String = move core::builtin::string_new(total);
            let mut i: i32 = 0;
            for ; (i < len_a); i = (i + 1) {
                let byte: i32 = "String::get"(&a, i);
                "String::set"(&mut result, i, byte);
            }
            let mut i: i32 = 0;
            for ; (i < len_b); i = (i + 1) {
                let byte: i32 = "String::get"(&b, i);
                "String::set"(&mut result, (len_a + i), byte);
            }
            break __inline_string_concat_12: result;
        };
        let b: String = move ")";
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_13: result;
    });
}

