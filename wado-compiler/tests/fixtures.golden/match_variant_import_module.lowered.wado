// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/match_variant_import_module.wado
// Generated by: make update-golden-fixtures

// Imports
// bundled::f64_to_buffer (f64_to_buffer)
// mem::realloc (realloc)
// wasi::stream-drop-writable (stream_drop_writable)
// wasi::stream-new (stream_new)
// wasi::stream-write (stream_write)
// wasi::subtask-drop (subtask_drop)
// wasi::task-return (task_return)
// wasi::waitable-join (waitable_join)
// wasi::waitable-set-new (waitable_set_new)
// wasi::waitable-set-wait (waitable_set_wait)

pub struct FormatSpec {
    fill: char,
    align: Align,
    width: i32,
}

pub fn apply_padding(s: String, spec: FormatSpec) -> String {
    let width: i32 = spec.width;
    if ((width <= 0) || (__inline_String__len_0: {
        break __inline_String__len_0: s.used;
    } >= width)) {
        return s;
    }
    let fill: char = spec.fill;
    let pad_len: i32 = (width - __inline_String__len_1: {
        break __inline_String__len_1: s.used;
    });
    let mut result: String = move "String::with_capacity"(width);
    return match spec.align {
        Left => {
            "String::append"(&mut result, s);
            for let mut i: i32 = 0; (i < pad_len); i = (i + 1) {
                "String::append_char"(&mut result, fill);
            }
            result;
        },
        Center => {
            let left_pad: i32 = (pad_len / 2);
            let right_pad: i32 = (pad_len - left_pad);
            for let mut i: i32 = 0; (i < left_pad); i = (i + 1) {
                "String::append_char"(&mut result, fill);
            }
            "String::append"(&mut result, s);
            for let mut i: i32 = 0; (i < right_pad); i = (i + 1) {
                "String::append_char"(&mut result, fill);
            }
            result;
        },
        Right => {
            for let mut i: i32 = 0; (i < pad_len); i = (i + 1) {
                "String::append_char"(&mut result, fill);
            }
            "String::append"(&mut result, s);
            result;
        },
    };
}

fn run() with Stdout {
    core::cli::println(__inline_f64_to_string_fill_0: {
        let value: f64 = 42.0;
        let align: Align = move Align::Right;
        let s: String = move "f64::to_string"(&value);
        let spec: FormatSpec = move FormatSpec { fill: '_', align: align, width: 8 };
        break __inline_f64_to_string_fill_0: apply_padding(s, spec);
    });
}

