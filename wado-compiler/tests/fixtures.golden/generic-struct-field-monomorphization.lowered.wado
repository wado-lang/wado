// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/generic-struct-field-monomorphization.wado
// Generated by: make update-golden-fixtures

struct "Container<i32>" {
    items: Array<i32>,
}

fn run() {
    let mut container: Container<i32> = move "Container<i32>::new"();
    __inline_Container_i32___add_0: {
        let self: &mut Container<i32> = &mut container;
        let item: i32 = 10;
        __inline_Array_i32___append_1: {
            let self: &mut Array<i32> = &mut self.items;
            let value: i32 = item;
            let used: i32 = self.used;
            let capacity: i32 = core::builtin::array_len(self.repr);
            if core::builtin::unlikely((used >= capacity)) {
                let mut new_capacity: i32 = (capacity * 2);
                if (capacity == 0) {
                    new_capacity = 4;
                }
                let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                self.repr = new_repr;
            }
            core::builtin::array_set::<i32>(self.repr, used, value);
            self.used = (used + 1);
        };
    };
    __inline_Container_i32___add_2: {
        let self: &mut Container<i32> = &mut container;
        let item: i32 = 20;
        __inline_Array_i32___append_3: {
            let self: &mut Array<i32> = &mut self.items;
            let value: i32 = item;
            let used: i32 = self.used;
            let capacity: i32 = core::builtin::array_len(self.repr);
            if core::builtin::unlikely((used >= capacity)) {
                let mut new_capacity: i32 = (capacity * 2);
                if (capacity == 0) {
                    new_capacity = 4;
                }
                let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                self.repr = new_repr;
            }
            core::builtin::array_set::<i32>(self.repr, used, value);
            self.used = (used + 1);
        };
    };
    __inline_Container_i32___add_4: {
        let self: &mut Container<i32> = &mut container;
        let item: i32 = 30;
        __inline_Array_i32___append_5: {
            let self: &mut Array<i32> = &mut self.items;
            let value: i32 = item;
            let used: i32 = self.used;
            let capacity: i32 = core::builtin::array_len(self.repr);
            if core::builtin::unlikely((used >= capacity)) {
                let mut new_capacity: i32 = (capacity * 2);
                if (capacity == 0) {
                    new_capacity = 4;
                }
                let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                self.repr = new_repr;
            }
            core::builtin::array_set::<i32>(self.repr, used, value);
            self.used = (used + 1);
        };
    };
    core::cli::println(__inline_string_concat_7: {
        let a: String = move "Length: ";
        let b: String = move "i32::to_string"(__inline_Container_i32___len_6: {
            let self: &Container<i32> = &container;
            break __inline_Container_i32___len_6: "Array<i32>::len"(&self.items);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_7: result;
    });
    core::cli::println(__inline_string_concat_9: {
        let a: String = move "First: ";
        let b: String = move "i32::to_string"(__inline_Container_i32___get_8: {
            let self: &Container<i32> = &container;
            let index: i32 = 0;
            break __inline_Container_i32___get_8: "Array<i32>^IndexValue::index_value"(&self.items, index);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_9: result;
    });
    core::cli::println(__inline_string_concat_11: {
        let a: String = move "Second: ";
        let b: String = move "i32::to_string"(__inline_Container_i32___get_10: {
            let self: &Container<i32> = &container;
            let index: i32 = 1;
            break __inline_Container_i32___get_10: "Array<i32>^IndexValue::index_value"(&self.items, index);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_11: result;
    });
    core::cli::println(__inline_string_concat_13: {
        let a: String = move "Third: ";
        let b: String = move "i32::to_string"(__inline_Container_i32___get_12: {
            let self: &Container<i32> = &container;
            let index: i32 = 2;
            break __inline_Container_i32___get_12: "Array<i32>^IndexValue::index_value"(&self.items, index);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_13: result;
    });
}

fn "Array<i32>^IndexValue::index_value"(self: &Array<i32>, index: i32) -> i32 {
    return core::builtin::array_get::<i32>(self.repr, index);
}

pub fn "Array<i32>::len"(self: &Array<i32>) -> i32 {
    return self.used;
}

fn "Container<i32>::new"() -> Container<i32> {
    return Container<i32> { items: [] };
}

