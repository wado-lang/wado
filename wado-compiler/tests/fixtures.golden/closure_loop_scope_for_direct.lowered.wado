// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/closure_loop_scope_for_direct.wado
// Generated by: make update-golden-fixtures

fn run() with Stdout {
    let mut closures: Array<fn(i32) -> i32> = move [];
    let mut i: i32 = 0;
    for ; (i < 5); i = (i + 1) {
        let get_i: fn(i32) -> i32 = |_: i32| captures[i] @capture[0]:i;
        __inline_Array_Fn_1_i32____append_0: {
            let self: &mut Array<fn(i32) -> i32> = &mut closures;
            let value: fn(i32) -> i32 = get_i;
            let used: i32 = self.used;
            let capacity: i32 = core::builtin::array_len(self.repr);
            if core::builtin::unlikely((used >= capacity)) {
                let mut new_capacity: i32 = (capacity * 2);
                if (capacity == 0) {
                    new_capacity = 4;
                }
                let new_repr: builtin::array<fn(i32) -> i32> = core::builtin::array_new::<fn(i32) -> i32>(new_capacity);
                core::builtin::array_copy::<fn(i32) -> i32>(new_repr, 0, self.repr, 0, used);
                self.repr = new_repr;
            }
            core::builtin::array_set::<fn(i32) -> i32>(self.repr, used, value);
            self.used = (used + 1);
        };
    }
    let r0: i32 = __inline_call_fn_1: {
        let f: fn(i32) -> i32 = __inline_Array_Fn_1_i32___IndexValue__index_value_2: {
            let self: &Array<fn(i32) -> i32> = &closures;
            let index: i32 = 0;
            break __inline_Array_Fn_1_i32___IndexValue__index_value_2: core::builtin::array_get::<fn(i32) -> i32>(self.repr, index);
        };
        let x: i32 = 0;
        break __inline_call_fn_1: f(x);
    };
    let r1: i32 = __inline_call_fn_3: {
        let f: fn(i32) -> i32 = __inline_Array_Fn_1_i32___IndexValue__index_value_4: {
            let self: &Array<fn(i32) -> i32> = &closures;
            let index: i32 = 1;
            break __inline_Array_Fn_1_i32___IndexValue__index_value_4: core::builtin::array_get::<fn(i32) -> i32>(self.repr, index);
        };
        let x: i32 = 0;
        break __inline_call_fn_3: f(x);
    };
    let r2: i32 = __inline_call_fn_5: {
        let f: fn(i32) -> i32 = __inline_Array_Fn_1_i32___IndexValue__index_value_6: {
            let self: &Array<fn(i32) -> i32> = &closures;
            let index: i32 = 2;
            break __inline_Array_Fn_1_i32___IndexValue__index_value_6: core::builtin::array_get::<fn(i32) -> i32>(self.repr, index);
        };
        let x: i32 = 0;
        break __inline_call_fn_5: f(x);
    };
    let r3: i32 = __inline_call_fn_7: {
        let f: fn(i32) -> i32 = __inline_Array_Fn_1_i32___IndexValue__index_value_8: {
            let self: &Array<fn(i32) -> i32> = &closures;
            let index: i32 = 3;
            break __inline_Array_Fn_1_i32___IndexValue__index_value_8: core::builtin::array_get::<fn(i32) -> i32>(self.repr, index);
        };
        let x: i32 = 0;
        break __inline_call_fn_7: f(x);
    };
    let r4: i32 = __inline_call_fn_9: {
        let f: fn(i32) -> i32 = __inline_Array_Fn_1_i32___IndexValue__index_value_10: {
            let self: &Array<fn(i32) -> i32> = &closures;
            let index: i32 = 4;
            break __inline_Array_Fn_1_i32___IndexValue__index_value_10: core::builtin::array_get::<fn(i32) -> i32>(self.repr, index);
        };
        let x: i32 = 0;
        break __inline_call_fn_9: f(x);
    };
    core::cli::println(__inline_string_concat_18: {
        let a: String = __inline_string_concat_17: {
            let a: String = __inline_string_concat_16: {
                let a: String = __inline_string_concat_15: {
                    let a: String = __inline_string_concat_14: {
                        let a: String = __inline_string_concat_13: {
                            let a: String = __inline_string_concat_12: {
                                let a: String = __inline_string_concat_11: {
                                    let a: String = move "i32::to_string"(r0);
                                    let b: String = move ",";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    let mut i: i32 = 0;
                                    for ; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    let mut i: i32 = 0;
                                    for ; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_11: result;
                                };
                                let b: String = move "i32::to_string"(r1);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                let mut i: i32 = 0;
                                for ; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                let mut i: i32 = 0;
                                for ; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_12: result;
                            };
                            let b: String = move ",";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            let mut i: i32 = 0;
                            for ; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            let mut i: i32 = 0;
                            for ; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_13: result;
                        };
                        let b: String = move "i32::to_string"(r2);
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        let mut i: i32 = 0;
                        for ; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        let mut i: i32 = 0;
                        for ; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_14: result;
                    };
                    let b: String = move ",";
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    let mut i: i32 = 0;
                    for ; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    let mut i: i32 = 0;
                    for ; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_15: result;
                };
                let b: String = move "i32::to_string"(r3);
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                let mut i: i32 = 0;
                for ; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                let mut i: i32 = 0;
                for ; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_16: result;
            };
            let b: String = move ",";
            let len_a: i32 = "String::len"(&a);
            let len_b: i32 = "String::len"(&b);
            let total: i32 = (len_a + len_b);
            let result: String = move core::builtin::string_new(total);
            let mut i: i32 = 0;
            for ; (i < len_a); i = (i + 1) {
                let byte: i32 = "String::get"(&a, i);
                "String::set"(&mut result, i, byte);
            }
            let mut i: i32 = 0;
            for ; (i < len_b); i = (i + 1) {
                let byte: i32 = "String::get"(&b, i);
                "String::set"(&mut result, (len_a + i), byte);
            }
            break __inline_string_concat_17: result;
        };
        let b: String = move "i32::to_string"(r4);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_18: result;
    });
}

