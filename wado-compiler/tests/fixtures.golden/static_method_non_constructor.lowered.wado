// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/static_method_non_constructor.wado
// Generated by: make update-golden-fixtures

struct Counter {
    value: i32,
}

pub fn "Counter::new"(value: i32) -> Counter {
    return Counter { value: value };
}

pub fn "Counter::zero"() -> i32 {
    return 0;
}

pub fn "Counter::from_pair"(a: i32, b: i32) -> Counter {
    return Counter { value: (a + b) };
}

fn run() with Stdout {
    let z: i32 = "Counter::zero"();
    core::cli::println(__inline_string_concat_0: {
        let a: String = move "zero: ";
        let b: String = move "i32::to_string"(z);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_0: result;
    });
    let c1: Counter = move "Counter::from_pair"(10, 20);
    core::cli::println(__inline_string_concat_2: {
        let a: String = move "from_pair: ";
        let b: String = move "i32::to_string"(__inline_Counter__get_1: {
            break __inline_Counter__get_1: c1.value;
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_2: result;
    });
    let c2: Counter = move "Counter::new"(42);
    core::cli::println(__inline_string_concat_4: {
        let a: String = move "counter value: ";
        let b: String = move "i32::to_string"(__inline_Counter__get_3: {
            break __inline_Counter__get_3: c2.value;
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_4: result;
    });
}

