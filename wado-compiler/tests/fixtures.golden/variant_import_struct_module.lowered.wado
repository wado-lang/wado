// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/variant_import_struct_module.wado
// Generated by: make update-golden-fixtures

// Imports
// mem::realloc (realloc)
// wasi::stream-drop-writable (stream_drop_writable)
// wasi::stream-new (stream_new)
// wasi::stream-write (stream_write)
// wasi::task-return (task_return)

pub struct Options {
    align: Align,
    width: i32,
}

pub fn "Options::default"() -> Options {
    return Options { align: Align::Right, width: 0 };
}

pub fn format_with_opts(s: String, opts: Options) -> String {
    if (opts.width <= 0) {
        return s;
    }
    if (__inline_String__len_0: {
        break __inline_String__len_0: s.used;
    } >= opts.width) {
        return s;
    }
    let pad_len: i32 = (opts.width - __inline_String__len_1: {
        break __inline_String__len_1: s.used;
    });
    let mut pad: String = move "String::with_capacity"(pad_len);
    for let mut i: i32 = 0; (i < pad_len); i = (i + 1) {
        "String::append"(&mut pad, move " ");
    }
    return match opts.align {
        Left => "String::concat"(s, pad),
        Right => "String::concat"(pad, s),
        Center => {
            let left: i32 = (pad_len / 2);
            let right: i32 = (pad_len - left);
            let mut lpad: String = move "String::with_capacity"(left);
            let mut rpad: String = move "String::with_capacity"(right);
            for let mut i: i32 = 0; (i < left); i = (i + 1) {
                "String::append"(&mut lpad, move " ");
            }
            for let mut i: i32 = 0; (i < right); i = (i + 1) {
                "String::append"(&mut rpad, move " ");
            }
            "String::concat"(move "String::concat"(lpad, s), rpad);
        },
    };
}

fn run() with Stdout {
    let opts: Options = move "Options::default"();
    let s: String = move format_with_opts(move "test", opts);
    core::cli::println(s);
}

