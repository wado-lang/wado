// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/struct_import_collision.wado
// Generated by: make update-golden-fixtures

struct Point {
    a: i32,
    b: i32,
    c: i32,
}

fn run() with Stdout {
    let local: Point = move Point { a: 1, b: 2, c: 3 };
    core::cli::println(__inline_string_concat_1: {
        let a: String = move "local total: ";
        let b: String = move "i32::to_string"(__inline_Point__total_0: {
            let self: &Point = &local;
            break __inline_Point__total_0: ((self.a + self.b) + self.c);
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_1: result;
    });
    let other: Point = move Point { x: 10, y: 4 };
    core::cli::println(__inline_string_concat_2: {
        let a: String = move "other sum: ";
        let b: String = move "i32::to_string"("Point::sum"(&other));
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_2: result;
    });
    core::cli::println(__inline_string_concat_3: {
        let a: String = move "other product: ";
        let b: String = move "i32::to_string"("Point::product"(&other));
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        let mut i: i32 = 0;
        for ; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        let mut i: i32 = 0;
        for ; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_3: result;
    });
}

