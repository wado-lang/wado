// Golden file: WIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/wasi_random.wado
// Generated by: make update-golden-fixtures

struct Box<i32> {  // Box<T> with T=i32
    mut value: i32,
}

array array<u8> (mut u8);

struct String {
    mut repr: ref array<u8>,
    mut used: i32,
}

struct [u64, u64] {
    mut 0: u64,
    mut 1: u64,
}

array array<u64> (mut u64);

struct Array<u64> {  // Array<T> with T=u64
    mut repr: ref array<u64>,
    mut used: i32,
}

type "functype/mem/realloc" = fn(i32, i32, i32, i32) -> i32;

type "functype/wasi/stream-drop-writable" = fn(i32);

type "functype/wasi/stream-new" = fn() -> i64;

type "functype/wasi/stream-write" = fn(i32, i32, i32) -> i32;

type "functype/wasi/subtask-drop" = fn(i32);

type "functype/wasi/task-return" = fn(i32);

type "functype/wasi/waitable-join" = fn(i32, i32);

type "functype/wasi/waitable-set-new" = fn() -> i32;

type "functype/wasi/waitable-set-wait" = fn(i32, i32) -> i32;

type "functype/wasi/wasi:cli/Stdout::write_via_stream" = fn(i32, i32) -> i32;

type "functype/wasi/wasi:random/InsecureSeed::get_insecure_seed" = fn(i32);

type "functype/wasi/wasi:random/Insecure::get_insecure_random_u64" = fn() -> i64;

type "functype/wasi/wasi:random/Random::get_random_u64" = fn() -> i64;

type "functype/run" = fn();

type "functype/__cm_adapter__Random_get_random_u64" = fn() -> u64;

type "functype/__cm_adapter__Insecure_get_insecure_random_u64" = fn() -> u64;

type "functype/__cm_adapter__InsecureSeed_get_insecure_seed" = fn() -> ref null "tuple/[u64, u64]";

type "functype/__cm_export__run" = fn();

type "functype/core:allocator/grow_memory" = fn(i32);

type "functype/core:allocator/bump_realloc" = fn(i32, i32, i32, i32) -> i32;

type "functype/core:cli/write_to_stream" = fn(i32, ref null String, bool);

type "functype/core:cli/println" = fn(ref null String);

type "functype/core:internal/wait_for_subtask" = fn(i32);

type "functype/__cm_adapter__InsecureSeed_get_insecure_seed" = fn() -> [u64, u64];

import fn realloc from "mem/realloc";
import fn stream-drop-writable from "wasi/stream-drop-writable";
import fn stream-new from "wasi/stream-new";
import fn stream-write from "wasi/stream-write";
import fn subtask-drop from "wasi/subtask-drop";
import fn task-return from "wasi/task-return";
import fn waitable-join from "wasi/waitable-join";
import fn waitable-set-new from "wasi/waitable-set-new";
import fn waitable-set-wait from "wasi/waitable-set-wait";
import fn wasi:cli/Stdout::write_via_stream from "wasi/wasi:cli/Stdout::write_via_stream";
import fn wasi:random/InsecureSeed::get_insecure_seed from "wasi/wasi:random/InsecureSeed::get_insecure_seed";
import fn wasi:random/Insecure::get_insecure_random_u64 from "wasi/wasi:random/Insecure::get_insecure_random_u64";
import fn wasi:random/Random::get_random_u64 from "wasi/wasi:random/Random::get_random_u64";
import memory (1) from "mem/memory";

global mut heap_offset: i32 = 8;  // from core:allocator
global mut __modules_initialized: bool = 0;

fn run() with Stdout, Random, Insecure, InsecureSeed {
    let r1: u64;
    let r2: u64;
    let ir1: u64;
    let ir2: u64;
    let seed: ref null "tuple/[u64, u64]";
    let seed_low: u64;
    let seed_high: u64;
    let __local_7: ref null Array<u64>;
    __inline___initialize_modules_0: block {
        if __modules_initialized {
            break __inline___initialize_modules_0;
        };
        __modules_initialized = 1;
    };
    r1 = __cm_adapter__Random_get_random_u64();
    r2 = __cm_adapter__Random_get_random_u64();
    "core:cli/println"(String { repr: array.new_data<u8>("test: random u64 works"), used: 22 });
    if if r1 != 0_i64 -> i32 {
        1;
    } else {
        r2 != 0_i64;
    } {
        "core:cli/println"(String { repr: array.new_data<u8>("test: random u64 produces non-zero values"), used: 41 });
    } else {
        "core:cli/println"(String { repr: array.new_data<u8>("test: random u64 produced two zeros (unlikely but valid)"), used: 56 });
    };
    if r1 != r2 {
        "core:cli/println"(String { repr: array.new_data<u8>("test: random u64 values differ"), used: 30 });
    } else {
        "core:cli/println"(String { repr: array.new_data<u8>("test: random u64 values same (unlikely but valid)"), used: 49 });
    };
    ir1 = __cm_adapter__Insecure_get_insecure_random_u64();
    ir2 = __cm_adapter__Insecure_get_insecure_random_u64();
    "core:cli/println"(String { repr: array.new_data<u8>("test: insecure random u64 works"), used: 31 });
    if ir1 != ir2 {
        "core:cli/println"(String { repr: array.new_data<u8>("test: insecure random u64 values differ"), used: 39 });
    } else {
        "core:cli/println"(String { repr: array.new_data<u8>("test: insecure random u64 values same (unlikely but valid)"), used: 58 });
    };
    let __sroa_seed_0: u64;
    let __sroa_seed_1: u64;
    multivalue_bind [__sroa_seed_0, __sroa_seed_1] = __cm_adapter__InsecureSeed_get_insecure_seed();
    seed_low = __sroa_seed_0;
    seed_high = __sroa_seed_1;
    "core:cli/println"(String { repr: array.new_data<u8>("test: insecure seed works"), used: 25 });
    if if seed_low != 0_i64 -> i32 {
        1;
    } else {
        seed_high != 0_i64;
    } {
        "core:cli/println"(String { repr: array.new_data<u8>("test: insecure seed produces non-zero value"), used: 43 });
    } else {
        "core:cli/println"(String { repr: array.new_data<u8>("test: insecure seed produced zeros (unlikely but valid)"), used: 55 });
    };
    "core:cli/println"(String { repr: array.new_data<u8>("all random tests passed"), used: 23 });
}

fn __cm_adapter__Random_get_random_u64() {
    return "wasi/wasi:random/Random::get_random_u64"();
}

fn __cm_adapter__Insecure_get_insecure_random_u64() {
    return "wasi/wasi:random/Insecure::get_insecure_random_u64"();
}

fn __cm_adapter__InsecureSeed_get_insecure_seed() {
    let __outptr: i32;
    __outptr = "mem/realloc"(0, 0, 8, 16);
    "wasi/wasi:random/InsecureSeed::get_insecure_seed"(__outptr);
    drop("mem/realloc"(__outptr, 16, 8, 0));
    return builtin::load_i64(__outptr + 0); builtin::load_i64(__outptr + 8);
}

fn __cm_export__run() {
    run();
    "wasi/task-return"(0);
}

fn "core:allocator/grow_memory"(new_offset) {  // from core:allocator
    let current_bytes: i32;
    let deficit: i32;
    let __local_3: i32;
    let normal_grow: i32;
    let grow_bytes: i32;
    let grow_pages: i32;
    current_bytes = builtin::memory_size() * 65536;
    deficit = new_offset - current_bytes;
    normal_grow = select i32(current_bytes < 16777216, current_bytes, 16777216);
    grow_bytes = if deficit > normal_grow -> i32 {
        1 << (32 - builtin::i32_clz(deficit - 1));
    } else {
        normal_grow;
    };
    grow_pages = (grow_bytes + 65535) / 65536;
    drop(builtin::memory_grow(grow_pages));
}

fn "core:allocator/bump_realloc"(oldptr, oldsize, align, newsize) {  // from core:allocator
    let aligned: i32;
    let new_offset: i32;
    if newsize == 0 {
        if (oldptr + oldsize) == heap_offset {
            heap_offset = oldptr;
        };
        return 0;
    };
    aligned = ((heap_offset + align) - 1) & (0 - align);
    new_offset = aligned + newsize;
    if builtin::unlikely(new_offset > (builtin::memory_size() * 65536)) {
        "core:allocator/grow_memory"(new_offset);
    };
    heap_offset = new_offset;
    return aligned;
}

fn "core:cli/write_to_stream"(tx, message, add_newline) {  // from core:cli
    let len: i32;
    let msg_bytes: ref null array<u8>;
    let alloc_size: i32;
    let ptr: i32;
    let i: i32;
    let byte: u8;
    let __local_9: ref null String;
    let __local_10: ref null String;
    len = message.used;
    msg_bytes = message.repr;
    alloc_size = if add_newline -> i32 {
        len + 1;
    } else {
        len;
    };
    ptr = "mem/realloc"(0, 0, 1, alloc_size);
    __for_0: block {
        i = 0;
        block {
            l13: loop {
                if (i < len) == 0 {
                    break __for_0;
                };
                byte = builtin::array_get_u8(msg_bytes, i);
                builtin::store_u8(ptr + i, byte);
                i = i + 1;
                continue l13;
            };
        };
    };
    if add_newline {
        builtin::store_u8(ptr + len, 10);
    };
    drop("wasi/stream-write"(tx, ptr, alloc_size));
    "wasi/stream-drop-writable"(tx);
}

fn "core:cli/println"(message) with Stdout {  // from core:cli
    let handles: i64;
    let rx: i32;
    let tx: i32;
    let subtask: i32;
    handles = "wasi/stream-new"();
    rx = builtin::i32_wrap_i64(handles);
    tx = builtin::i32_wrap_i64(handles >> 32_i64);
    subtask = "wasi/wasi:cli/Stdout::write_via_stream"(rx, 2048);
    "core:cli/write_to_stream"(tx, message, 1);
    "core:internal/wait_for_subtask"(subtask);
}

fn "core:internal/wait_for_subtask"(packed) {  // from core:internal
    let handle: i32;
    let ws: i32;
    let ptr: i32;
    handle = packed >> 4;
    if handle != 0 {
        ws = "wasi/waitable-set-new"();
        "wasi/waitable-join"(handle, ws);
        ptr = "mem/realloc"(0, 0, 4, 8);
        drop("wasi/waitable-set-wait"(ws, ptr));
        drop("mem/realloc"(ptr, 8, 4, 0));
        "wasi/subtask-drop"(handle);
    };
}

export fn __cm_export__run as "run"
