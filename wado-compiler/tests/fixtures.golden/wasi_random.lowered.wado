// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/wasi_random.wado
// Generated by: make update-golden-fixtures

// Imports
// mem::realloc (realloc)
// wasi::stream-drop-writable (stream_drop_writable)
// wasi::stream-new (stream_new)
// wasi::stream-write (stream_write)
// wasi::task-return (task_return)

fn run() with Stdout, Random, Insecure, InsecureSeed {
    let r1: u64 = Random::get_random_u64();
    let r2: u64 = Random::get_random_u64();
    core::cli::println(move "test: random u64 works");
    if ((r1 != 0) || (r2 != 0)) {
        core::cli::println(move "test: random u64 produces non-zero values");
    } else {
        core::cli::println(move "test: random u64 produced two zeros (unlikely but valid)");
    }
    if (r1 != r2) {
        core::cli::println(move "test: random u64 values differ");
    } else {
        core::cli::println(move "test: random u64 values same (unlikely but valid)");
    }
    let ir1: u64 = Insecure::get_insecure_random_u64();
    let ir2: u64 = Insecure::get_insecure_random_u64();
    core::cli::println(move "test: insecure random u64 works");
    if (ir1 != ir2) {
        core::cli::println(move "test: insecure random u64 values differ");
    } else {
        core::cli::println(move "test: insecure random u64 values same (unlikely but valid)");
    }
    let seed: [u64, u64] = move InsecureSeed::get_insecure_seed();
    let seed_low: u64 = seed.0;
    let seed_high: u64 = seed.1;
    core::cli::println(move "test: insecure seed works");
    if ((seed_low != 0) || (seed_high != 0)) {
        core::cli::println(move "test: insecure seed produces non-zero value");
    } else {
        core::cli::println(move "test: insecure seed produced zeros (unlikely but valid)");
    }
    core::cli::println(move "all random tests passed");
}

