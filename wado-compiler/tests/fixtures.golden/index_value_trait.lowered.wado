// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/index_value_trait.wado
// Generated by: make update-golden-fixtures

struct IntStore {
    a: i32,
    b: i32,
    c: i32,
}

fn "IntStore^IndexValue::index_value"(self: &IntStore, index: i32) -> i32 {
    if (index == 0) {
        return self.a;
    } else {
        if (index == 1) {
            return self.b;
        } else {
            return self.c;
        }
    }
}

fn "IntStore::new"(a: i32, b: i32, c: i32) -> IntStore {
    return IntStore { a: a, b: b, c: c };
}

fn run() with Stdout {
    let store: IntStore = move "IntStore::new"(10, 20, 30);
    let first: i32 = "IntStore^IndexValue::index_value"(&store, 0);
    let second: i32 = "IntStore^IndexValue::index_value"(&store, 1);
    let third: i32 = "IntStore^IndexValue::index_value"(&store, 2);
    core::cli::println(__inline_string_concat_0: {
        let a: String = move "first: ";
        let b: String = move "i32::to_string"(first);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_0: result;
    });
    core::cli::println(__inline_string_concat_1: {
        let a: String = move "second: ";
        let b: String = move "i32::to_string"(second);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_1: result;
    });
    core::cli::println(__inline_string_concat_2: {
        let a: String = move "third: ";
        let b: String = move "i32::to_string"(third);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_2: result;
    });
    let sum: i32 = (("IntStore^IndexValue::index_value"(&store, 0) + "IntStore^IndexValue::index_value"(&store, 1)) + "IntStore^IndexValue::index_value"(&store, 2));
    core::cli::println(__inline_string_concat_3: {
        let a: String = move "sum: ";
        let b: String = move "i32::to_string"(sum);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_3: result;
    });
}

