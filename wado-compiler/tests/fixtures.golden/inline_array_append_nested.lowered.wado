// Golden file: Lowered TIR with -O2 optimization
// Source: tests/fixtures/inline_array_append_nested.wado
// Generated by: make update-golden-fixtures

struct Container {
    items: Array<i32>,
}

fn "Container::add_items"(self: &mut Container, count: i32) {
    let mut i: i32 = 0;
    for ; (i < count); i = (i + 1) {
        __inline_Array_i32___append_0: {
            let self: &mut Array<i32> = &mut self.items;
            let value: i32 = i;
            let used: i32 = self.used;
            let capacity: i32 = core::builtin::array_len(self.repr);
            if core::builtin::unlikely((used >= capacity)) {
                let mut new_capacity: i32 = (capacity * 2);
                if (capacity == 0) {
                    new_capacity = 4;
                }
                let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                self.repr = new_repr;
            }
            core::builtin::array_set::<i32>(self.repr, used, value);
            self.used = (used + 1);
        }
        __inline_Array_i32___append_1: {
            let self: &mut Array<i32> = &mut self.items;
            let value: i32 = (i * 2);
            let used: i32 = self.used;
            let capacity: i32 = core::builtin::array_len(self.repr);
            if core::builtin::unlikely((used >= capacity)) {
                let mut new_capacity: i32 = (capacity * 2);
                if (capacity == 0) {
                    new_capacity = 4;
                }
                let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                self.repr = new_repr;
            }
            core::builtin::array_set::<i32>(self.repr, used, value);
            self.used = (used + 1);
        }
    }
}

fn "Container::process"(self: &mut Container) {
    let mut temp: Array<i32> = move [];
    let mut i: i32 = 0;
    let _licm_items: Array<i32> = self.items;
    for ; (i < "Array<i32>::len"(&self.items)); i = (i + 1) {
        let val: i32 = "Array<i32>^IndexValue::index_value"(&_licm_items, i);
        if ((val % 2) == 0) {
            __inline_Array_i32___append_0: {
                let self: &mut Array<i32> = &mut temp;
                let value: i32 = val;
                let used: i32 = self.used;
                let capacity: i32 = core::builtin::array_len(self.repr);
                if core::builtin::unlikely((used >= capacity)) {
                    let mut new_capacity: i32 = (capacity * 2);
                    if (capacity == 0) {
                        new_capacity = 4;
                    }
                    let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                    core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                    self.repr = new_repr;
                }
                core::builtin::array_set::<i32>(self.repr, used, value);
                self.used = (used + 1);
            }
        }
    }
    let mut j: i32 = 0;
    for ; (j < "Array<i32>::len"(&temp)); j = (j + 1) {
        __inline_Array_i32___append_1: {
            let self: &mut Array<i32> = &mut self.items;
            let value: i32 = ("Array<i32>^IndexValue::index_value"(&temp, j) + 100);
            let used: i32 = self.used;
            let capacity: i32 = core::builtin::array_len(self.repr);
            if core::builtin::unlikely((used >= capacity)) {
                let mut new_capacity: i32 = (capacity * 2);
                if (capacity == 0) {
                    new_capacity = 4;
                }
                let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                self.repr = new_repr;
            }
            core::builtin::array_set::<i32>(self.repr, used, value);
            self.used = (used + 1);
        }
    }
}

fn run() with Stdout {
    let mut c: Container = move Container { items: [] };
    "Container::add_items"(&mut c, 3);
    "Container::process"(&mut c);
    core::cli::println(move core::internal::string_concat(move "len=", move "i32::to_string"("Array<i32>::len"(&c.items))));
}

fn "Array<i32>^IndexValue::index_value"(self: &Array<i32>, index: i32) -> i32 {
    return core::builtin::array_get::<i32>(self.repr, index);
}

pub fn "Array<i32>::len"(self: &Array<i32>) -> i32 {
    return self.used;
}

