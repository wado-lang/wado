// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/inline_array_append_nested.wado
// Generated by: make update-golden-fixtures

struct Container {
    items: Array<i32>,
}

fn run() with Stdout {
    let mut c: Container = move Container { items: [] };
    __inline_Container__add_items_0: {
        for let mut i: i32 = 0; (i < 3); i = (i + 1) {
            __inline_Array_i32___append_1: {
                let self: &mut Array<i32> = &mut c.items;
                let used: i32 = self.used;
                let capacity: i32 = core::builtin::array_len(self.repr);
                if core::builtin::unlikely((used >= capacity)) {
                    let mut new_capacity: i32 = (capacity * 2);
                    if (capacity == 0) {
                        new_capacity = 4;
                    }
                    let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                    core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                    self.repr = new_repr;
                }
                core::builtin::array_set::<i32>(self.repr, used, i);
                self.used = (used + 1);
            };
            __inline_Array_i32___append_2: {
                let self: &mut Array<i32> = &mut c.items;
                let value: i32 = (i * 2);
                let used: i32 = self.used;
                let capacity: i32 = core::builtin::array_len(self.repr);
                if core::builtin::unlikely((used >= capacity)) {
                    let mut new_capacity: i32 = (capacity * 2);
                    if (capacity == 0) {
                        new_capacity = 4;
                    }
                    let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                    core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                    self.repr = new_repr;
                }
                core::builtin::array_set::<i32>(self.repr, used, value);
                self.used = (used + 1);
            };
        }
    };
    __inline_Container__process_3: {
        let mut temp: Array<i32> = move [];
        for let mut i: i32 = 0; (i < "Array<i32>::len"(&c.items)); i = (i + 1) {
            let val: i32 = __inline_Array_i32__IndexValue__index_value_4: {
                let self: &Array<i32> = &c.items;
                break __inline_Array_i32__IndexValue__index_value_4: core::builtin::array_get::<i32>(self.repr, i);
            };
            if ((val % 2) == 0) {
                __inline_Array_i32___append_5: {
                    let used: i32 = temp.used;
                    let capacity: i32 = core::builtin::array_len(temp.repr);
                    if core::builtin::unlikely((used >= capacity)) {
                        let mut new_capacity: i32 = (capacity * 2);
                        if (capacity == 0) {
                            new_capacity = 4;
                        }
                        let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                        core::builtin::array_copy::<i32>(new_repr, 0, temp.repr, 0, used);
                        temp.repr = new_repr;
                    }
                    core::builtin::array_set::<i32>(temp.repr, used, val);
                    temp.used = (used + 1);
                };
            }
        }
        for let mut j: i32 = 0; (j < "Array<i32>::len"(&temp)); j = (j + 1) {
            __inline_Array_i32___append_6: {
                let self: &mut Array<i32> = &mut c.items;
                let value: i32 = (__inline_Array_i32__IndexValue__index_value_7: {
                    break __inline_Array_i32__IndexValue__index_value_7: core::builtin::array_get::<i32>(temp.repr, j);
                } + 100);
                let used: i32 = self.used;
                let capacity: i32 = core::builtin::array_len(self.repr);
                if core::builtin::unlikely((used >= capacity)) {
                    let mut new_capacity: i32 = (capacity * 2);
                    if (capacity == 0) {
                        new_capacity = 4;
                    }
                    let new_repr: builtin::array<i32> = core::builtin::array_new::<i32>(new_capacity);
                    core::builtin::array_copy::<i32>(new_repr, 0, self.repr, 0, used);
                    self.repr = new_repr;
                }
                core::builtin::array_set::<i32>(self.repr, used, value);
                self.used = (used + 1);
            };
        }
    };
    core::cli::println(__inline_string_concat_9: {
        let a: String = move "len=";
        let b: String = move "i32::to_string"(__inline_Array_i32___len_8: {
            let self: &Array<i32> = &c.items;
            break __inline_Array_i32___len_8: self.used;
        });
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_9: result;
    });
}

pub fn "Array<i32>::len"(self: &Array<i32>) -> i32 {
    return self.used;
}

