// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado
// Generated by: make update-golden-fixtures

fn run() {
    let a: i32 = (1 << (2 + 1));
    __assert_0: {
        let __cond: bool = (a == 8);
        if !__cond {
            core::prelude::panic(__inline_string_concat_8: {
                let a: String = __inline_string_concat_7: {
                    let a: String = __inline_string_concat_6: {
                        let a: String = __inline_string_concat_5: {
                            let a: String = __inline_string_concat_4: {
                                let a: String = __inline_string_concat_3: {
                                    let a: String = __inline_string_concat_2: {
                                        let a: String = __inline_string_concat_1: {
                                            let a: String = __inline_string_concat_0: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_0: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_1: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_2: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_3: result;
                                };
                                let b: String = move "i32::to_string"(8);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_4: result;
                            };
                            let b: String = move "\ncondition: a == 8\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_5: result;
                        };
                        let b: String = move "a: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_6: result;
                    };
                    let b: String = move "i32::to_string"(a);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_7: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_8: result;
            });
        }
    }
    let b: i32 = (16 >> (4 - 2));
    __assert_1: {
        let __cond: bool = (b == 4);
        if !__cond {
            core::prelude::panic(__inline_string_concat_17: {
                let a: String = __inline_string_concat_16: {
                    let a: String = __inline_string_concat_15: {
                        let a: String = __inline_string_concat_14: {
                            let a: String = __inline_string_concat_13: {
                                let a: String = __inline_string_concat_12: {
                                    let a: String = __inline_string_concat_11: {
                                        let a: String = __inline_string_concat_10: {
                                            let a: String = __inline_string_concat_9: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_9: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_10: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_11: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_12: result;
                                };
                                let b: String = move "i32::to_string"(13);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_13: result;
                            };
                            let b: String = move "\ncondition: b == 4\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_14: result;
                        };
                        let b: String = move "b: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_15: result;
                    };
                    let b: String = move "i32::to_string"(b);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_16: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_17: result;
            });
        }
    }
    let c: i32 = (1 << (2 * 2));
    __assert_2: {
        let __cond: bool = (c == 16);
        if !__cond {
            core::prelude::panic(__inline_string_concat_26: {
                let a: String = __inline_string_concat_25: {
                    let a: String = __inline_string_concat_24: {
                        let a: String = __inline_string_concat_23: {
                            let a: String = __inline_string_concat_22: {
                                let a: String = __inline_string_concat_21: {
                                    let a: String = __inline_string_concat_20: {
                                        let a: String = __inline_string_concat_19: {
                                            let a: String = __inline_string_concat_18: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_18: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_19: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_20: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_21: result;
                                };
                                let b: String = move "i32::to_string"(18);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_22: result;
                            };
                            let b: String = move "\ncondition: c == 16\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_23: result;
                        };
                        let b: String = move "c: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_24: result;
                    };
                    let b: String = move "i32::to_string"(c);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_25: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_26: result;
            });
        }
    }
    let d: i32 = (1 << (1 + 2));
    __assert_3: {
        let __cond: bool = (d == 8);
        if !__cond {
            core::prelude::panic(__inline_string_concat_35: {
                let a: String = __inline_string_concat_34: {
                    let a: String = __inline_string_concat_33: {
                        let a: String = __inline_string_concat_32: {
                            let a: String = __inline_string_concat_31: {
                                let a: String = __inline_string_concat_30: {
                                    let a: String = __inline_string_concat_29: {
                                        let a: String = __inline_string_concat_28: {
                                            let a: String = __inline_string_concat_27: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_27: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_28: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_29: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_30: result;
                                };
                                let b: String = move "i32::to_string"(28);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_31: result;
                            };
                            let b: String = move "\ncondition: d == 8\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_32: result;
                        };
                        let b: String = move "d: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_33: result;
                    };
                    let b: String = move "i32::to_string"(d);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_34: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_35: result;
            });
        }
    }
    let e: i32 = ((1 << 2) + 3);
    __assert_4: {
        let __cond: bool = (e == 7);
        if !__cond {
            core::prelude::panic(__inline_string_concat_44: {
                let a: String = __inline_string_concat_43: {
                    let a: String = __inline_string_concat_42: {
                        let a: String = __inline_string_concat_41: {
                            let a: String = __inline_string_concat_40: {
                                let a: String = __inline_string_concat_39: {
                                    let a: String = __inline_string_concat_38: {
                                        let a: String = __inline_string_concat_37: {
                                            let a: String = __inline_string_concat_36: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_36: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_37: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_38: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_39: result;
                                };
                                let b: String = move "i32::to_string"(33);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_40: result;
                            };
                            let b: String = move "\ncondition: e == 7\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_41: result;
                        };
                        let b: String = move "e: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_42: result;
                    };
                    let b: String = move "i32::to_string"(e);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_43: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_44: result;
            });
        }
    }
    let f: i32 = ((64 >> 2) >> 1);
    __assert_5: {
        let __cond: bool = (f == 8);
        if !__cond {
            core::prelude::panic(__inline_string_concat_53: {
                let a: String = __inline_string_concat_52: {
                    let a: String = __inline_string_concat_51: {
                        let a: String = __inline_string_concat_50: {
                            let a: String = __inline_string_concat_49: {
                                let a: String = __inline_string_concat_48: {
                                    let a: String = __inline_string_concat_47: {
                                        let a: String = __inline_string_concat_46: {
                                            let a: String = __inline_string_concat_45: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_45: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_46: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_47: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_48: result;
                                };
                                let b: String = move "i32::to_string"(38);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_49: result;
                            };
                            let b: String = move "\ncondition: f == 8\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_50: result;
                        };
                        let b: String = move "f: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_51: result;
                    };
                    let b: String = move "i32::to_string"(f);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_52: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_53: result;
            });
        }
    }
    let g: i32 = ((1 << 2) << 1);
    __assert_6: {
        let __cond: bool = (g == 8);
        if !__cond {
            core::prelude::panic(__inline_string_concat_62: {
                let a: String = __inline_string_concat_61: {
                    let a: String = __inline_string_concat_60: {
                        let a: String = __inline_string_concat_59: {
                            let a: String = __inline_string_concat_58: {
                                let a: String = __inline_string_concat_57: {
                                    let a: String = __inline_string_concat_56: {
                                        let a: String = __inline_string_concat_55: {
                                            let a: String = __inline_string_concat_54: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_54: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_55: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_56: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_57: result;
                                };
                                let b: String = move "i32::to_string"(42);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_58: result;
                            };
                            let b: String = move "\ncondition: g == 8\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_59: result;
                        };
                        let b: String = move "g: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_60: result;
                    };
                    let b: String = move "i32::to_string"(g);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_61: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_62: result;
            });
        }
    }
    let h: i32 = (8 & (4 << 1));
    __assert_7: {
        let __cond: bool = (h == 8);
        if !__cond {
            core::prelude::panic(__inline_string_concat_71: {
                let a: String = __inline_string_concat_70: {
                    let a: String = __inline_string_concat_69: {
                        let a: String = __inline_string_concat_68: {
                            let a: String = __inline_string_concat_67: {
                                let a: String = __inline_string_concat_66: {
                                    let a: String = __inline_string_concat_65: {
                                        let a: String = __inline_string_concat_64: {
                                            let a: String = __inline_string_concat_63: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_63: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_64: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_65: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_66: result;
                                };
                                let b: String = move "i32::to_string"(48);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_67: result;
                            };
                            let b: String = move "\ncondition: h == 8\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_68: result;
                        };
                        let b: String = move "h: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_69: result;
                    };
                    let b: String = move "i32::to_string"(h);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_70: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_71: result;
            });
        }
    }
    let i: i32 = ((15 >> 1) & 3);
    __assert_8: {
        let __cond: bool = (i == 3);
        if !__cond {
            core::prelude::panic(__inline_string_concat_80: {
                let a: String = __inline_string_concat_79: {
                    let a: String = __inline_string_concat_78: {
                        let a: String = __inline_string_concat_77: {
                            let a: String = __inline_string_concat_76: {
                                let a: String = __inline_string_concat_75: {
                                    let a: String = __inline_string_concat_74: {
                                        let a: String = __inline_string_concat_73: {
                                            let a: String = __inline_string_concat_72: {
                                                let a: String = move "Assertion failed in ";
                                                let b: String = move "run";
                                                let len_a: i32 = "String::len"(&a);
                                                let len_b: i32 = "String::len"(&b);
                                                let total: i32 = (len_a + len_b);
                                                let result: String = move core::builtin::string_new(total);
                                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&a, i);
                                                    "String::set"(&mut result, i, byte);
                                                }
                                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                    let byte: i32 = "String::get"(&b, i);
                                                    "String::set"(&mut result, (len_a + i), byte);
                                                }
                                                break __inline_string_concat_72: result;
                                            };
                                            let b: String = move " at ";
                                            let len_a: i32 = "String::len"(&a);
                                            let len_b: i32 = "String::len"(&b);
                                            let total: i32 = (len_a + len_b);
                                            let result: String = move core::builtin::string_new(total);
                                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                                let byte: i32 = "String::get"(&a, i);
                                                "String::set"(&mut result, i, byte);
                                            }
                                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                                let byte: i32 = "String::get"(&b, i);
                                                "String::set"(&mut result, (len_a + i), byte);
                                            }
                                            break __inline_string_concat_73: result;
                                        };
                                        let b: String = move "wado-compiler/tests/fixtures/precedence_shift_vs_arithmetic.wado";
                                        let len_a: i32 = "String::len"(&a);
                                        let len_b: i32 = "String::len"(&b);
                                        let total: i32 = (len_a + len_b);
                                        let result: String = move core::builtin::string_new(total);
                                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                            let byte: i32 = "String::get"(&a, i);
                                            "String::set"(&mut result, i, byte);
                                        }
                                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                            let byte: i32 = "String::get"(&b, i);
                                            "String::set"(&mut result, (len_a + i), byte);
                                        }
                                        break __inline_string_concat_74: result;
                                    };
                                    let b: String = move ":";
                                    let len_a: i32 = "String::len"(&a);
                                    let len_b: i32 = "String::len"(&b);
                                    let total: i32 = (len_a + len_b);
                                    let result: String = move core::builtin::string_new(total);
                                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                        let byte: i32 = "String::get"(&a, i);
                                        "String::set"(&mut result, i, byte);
                                    }
                                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                        let byte: i32 = "String::get"(&b, i);
                                        "String::set"(&mut result, (len_a + i), byte);
                                    }
                                    break __inline_string_concat_75: result;
                                };
                                let b: String = move "i32::to_string"(54);
                                let len_a: i32 = "String::len"(&a);
                                let len_b: i32 = "String::len"(&b);
                                let total: i32 = (len_a + len_b);
                                let result: String = move core::builtin::string_new(total);
                                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                    let byte: i32 = "String::get"(&a, i);
                                    "String::set"(&mut result, i, byte);
                                }
                                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                    let byte: i32 = "String::get"(&b, i);
                                    "String::set"(&mut result, (len_a + i), byte);
                                }
                                break __inline_string_concat_76: result;
                            };
                            let b: String = move "\ncondition: i == 3\n";
                            let len_a: i32 = "String::len"(&a);
                            let len_b: i32 = "String::len"(&b);
                            let total: i32 = (len_a + len_b);
                            let result: String = move core::builtin::string_new(total);
                            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                                let byte: i32 = "String::get"(&a, i);
                                "String::set"(&mut result, i, byte);
                            }
                            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                                let byte: i32 = "String::get"(&b, i);
                                "String::set"(&mut result, (len_a + i), byte);
                            }
                            break __inline_string_concat_77: result;
                        };
                        let b: String = move "i: ";
                        let len_a: i32 = "String::len"(&a);
                        let len_b: i32 = "String::len"(&b);
                        let total: i32 = (len_a + len_b);
                        let result: String = move core::builtin::string_new(total);
                        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                            let byte: i32 = "String::get"(&a, i);
                            "String::set"(&mut result, i, byte);
                        }
                        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                            let byte: i32 = "String::get"(&b, i);
                            "String::set"(&mut result, (len_a + i), byte);
                        }
                        break __inline_string_concat_78: result;
                    };
                    let b: String = move "i32::to_string"(i);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_79: result;
                };
                let b: String = move "\n";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_80: result;
            });
        }
    }
}

