// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/enum-eq-ord.wado
// Generated by: make update-golden-fixtures

// Imports
// mem::realloc (realloc)
// wasi::stream-drop-writable (stream_drop_writable)
// wasi::stream-new (stream_new)
// wasi::stream-write (stream_write)
// wasi::subtask-drop (subtask_drop)
// wasi::task-return (task_return)
// wasi::waitable-join (waitable_join)
// wasi::waitable-set-new (waitable_set_new)
// wasi::waitable-set-wait (waitable_set_wait)

enum Priority {
    Low,
    Medium,
    High,
    Critical,
}

export fn run() with Stdout {
    let a: Priority = Priority::Low;
    let b: Priority = Priority::Medium;
    let c: Priority = Priority::Low;
    if (a == c) {
        core::cli::println(move "low == low");
    }
    if (a != b) {
        core::cli::println(move "low != medium");
    }
    if (a == b) {
        core::cli::println(move "BUG: low == medium");
    }
    if (a < b) {
        core::cli::println(move "low < medium");
    }
    if (b > a) {
        core::cli::println(move "medium > low");
    }
    if (a <= c) {
        core::cli::println(move "low <= low");
    }
    if (a >= c) {
        core::cli::println(move "low >= low");
    }
    let h: Priority = Priority::High;
    let cr: Priority = Priority::Critical;
    if (h < cr) {
        core::cli::println(move "high < critical");
    }
    if ((a < b) && (b < h)) {
        core::cli::println(move "low < medium < high");
    }
}

export fn __cm_export__run() {
    run();
    cm_raw_call task-return(0);
}

