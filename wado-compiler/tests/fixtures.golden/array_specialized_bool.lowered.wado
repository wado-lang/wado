// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/array_specialized_bool.wado
// Generated by: make update-golden-fixtures

fn run() with Stdout {
    let mut arr: Array<bool> = move [];
    __inline_Array_bool___append_0: {
        let used: i32 = arr.used;
        let capacity: i32 = core::builtin::array_len(arr.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<bool> = core::builtin::array_new::<bool>(new_capacity);
            core::builtin::array_copy::<bool>(new_repr, 0, arr.repr, 0, used);
            arr.repr = new_repr;
        }
        core::builtin::array_set::<bool>(arr.repr, used, true);
        arr.used = (used + 1);
    };
    __inline_Array_bool___append_1: {
        let used: i32 = arr.used;
        let capacity: i32 = core::builtin::array_len(arr.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<bool> = core::builtin::array_new::<bool>(new_capacity);
            core::builtin::array_copy::<bool>(new_repr, 0, arr.repr, 0, used);
            arr.repr = new_repr;
        }
        core::builtin::array_set::<bool>(arr.repr, used, false);
        arr.used = (used + 1);
    };
    __inline_Array_bool___append_2: {
        let used: i32 = arr.used;
        let capacity: i32 = core::builtin::array_len(arr.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<bool> = core::builtin::array_new::<bool>(new_capacity);
            core::builtin::array_copy::<bool>(new_repr, 0, arr.repr, 0, used);
            arr.repr = new_repr;
        }
        core::builtin::array_set::<bool>(arr.repr, used, true);
        arr.used = (used + 1);
    };
    for let _item of arr {
        core::cli::println(move "bool::to_string"(x));
    }
}

