// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/array_specialized_f32.wado
// Generated by: make update-golden-fixtures

fn run() with Stdout {
    let mut arr: Array<f32> = move [];
    __inline_Array_f32___append_0: {
        let value: f32 = 1.5 as f32;
        let used: i32 = arr.used;
        let capacity: i32 = core::builtin::array_len(arr.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<f32> = core::builtin::array_new::<f32>(new_capacity);
            core::builtin::array_copy::<f32>(new_repr, 0, arr.repr, 0, used);
            arr.repr = new_repr;
        }
        core::builtin::array_set::<f32>(arr.repr, used, value);
        arr.used = (used + 1);
    };
    __inline_Array_f32___append_1: {
        let value: f32 = 2.5 as f32;
        let used: i32 = arr.used;
        let capacity: i32 = core::builtin::array_len(arr.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<f32> = core::builtin::array_new::<f32>(new_capacity);
            core::builtin::array_copy::<f32>(new_repr, 0, arr.repr, 0, used);
            arr.repr = new_repr;
        }
        core::builtin::array_set::<f32>(arr.repr, used, value);
        arr.used = (used + 1);
    };
    __inline_Array_f32___append_2: {
        let value: f32 = 3.5 as f32;
        let used: i32 = arr.used;
        let capacity: i32 = core::builtin::array_len(arr.repr);
        if core::builtin::unlikely((used >= capacity)) {
            let mut new_capacity: i32 = (capacity * 2);
            if (capacity == 0) {
                new_capacity = 4;
            }
            let new_repr: builtin::array<f32> = core::builtin::array_new::<f32>(new_capacity);
            core::builtin::array_copy::<f32>(new_repr, 0, arr.repr, 0, used);
            arr.repr = new_repr;
        }
        core::builtin::array_set::<f32>(arr.repr, used, value);
        arr.used = (used + 1);
    };
    for let _item of arr {
        core::cli::println(move "f32::to_string"(x));
    }
}

