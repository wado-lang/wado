// Golden file: Lowered TIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/short_circuit.wado
// Generated by: make update-golden-fixtures

fn side_effect(msg: String, result: bool) -> bool with Stdout {
    core::cli::println(__inline_string_concat_0: {
        let a: String = move "side_effect called: ";
        let b: String = msg;
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_0: result;
    });
    return result;
}

fn run() with Stdout {
    core::cli::println(move "=== Testing && ===");
    if (false && side_effect(move "should not print (&&)", true)) {
        core::cli::println(move "branch taken");
    } else {
        core::cli::println(move "branch not taken (correct)");
    }
    if (true && side_effect(move "should print (&&)", true)) {
        core::cli::println(move "branch taken (correct)");
    }
    core::cli::println(move "\n=== Testing || ===");
    if (true || side_effect(move "should not print (||)", false)) {
        core::cli::println(move "branch taken (correct)");
    }
    if (false || side_effect(move "should print (||)", true)) {
        core::cli::println(move "branch taken (correct)");
    }
    core::cli::println(move "\n=== Testing while loop ===");
    let arr: Array<i32> = move [10, 20, 30];
    let mut i: i32 = 0;
    let _licm_repr: builtin::array<i32> = arr.repr;
    while ((i < "Array<i32>::len"(&arr)) && ("Array<i32>^IndexValue::index_value"(&arr, i) < 100)) {
        core::cli::println(__inline_string_concat_3: {
            let a: String = __inline_string_concat_1: {
                let a: String = __inline_string_concat_0: {
                    let a: String = move "arr[";
                    let b: String = move "i32::to_string"(i);
                    let len_a: i32 = "String::len"(&a);
                    let len_b: i32 = "String::len"(&b);
                    let total: i32 = (len_a + len_b);
                    let result: String = move core::builtin::string_new(total);
                    for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                        let byte: i32 = "String::get"(&a, i);
                        "String::set"(&mut result, i, byte);
                    }
                    for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                        let byte: i32 = "String::get"(&b, i);
                        "String::set"(&mut result, (len_a + i), byte);
                    }
                    break __inline_string_concat_0: result;
                };
                let b: String = move "] = ";
                let len_a: i32 = "String::len"(&a);
                let len_b: i32 = "String::len"(&b);
                let total: i32 = (len_a + len_b);
                let result: String = move core::builtin::string_new(total);
                for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                    let byte: i32 = "String::get"(&a, i);
                    "String::set"(&mut result, i, byte);
                }
                for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                    let byte: i32 = "String::get"(&b, i);
                    "String::set"(&mut result, (len_a + i), byte);
                }
                break __inline_string_concat_1: result;
            };
            let b: String = move "i32::to_string"(__inline_Array_i32__IndexValue__index_value_2: {
                break __inline_Array_i32__IndexValue__index_value_2: core::builtin::array_get::<i32>(_licm_repr, i);
            });
            let len_a: i32 = "String::len"(&a);
            let len_b: i32 = "String::len"(&b);
            let total: i32 = (len_a + len_b);
            let result: String = move core::builtin::string_new(total);
            for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
                let byte: i32 = "String::get"(&a, i);
                "String::set"(&mut result, i, byte);
            }
            for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
                let byte: i32 = "String::get"(&b, i);
                "String::set"(&mut result, (len_a + i), byte);
            }
            break __inline_string_concat_3: result;
        });
        i = (i + 1);
    }
    core::cli::println(__inline_string_concat_4: {
        let a: String = move "Loop ended at i = ";
        let b: String = move "i32::to_string"(i);
        let len_a: i32 = "String::len"(&a);
        let len_b: i32 = "String::len"(&b);
        let total: i32 = (len_a + len_b);
        let result: String = move core::builtin::string_new(total);
        for let mut i: i32 = 0; (i < len_a); i = (i + 1) {
            let byte: i32 = "String::get"(&a, i);
            "String::set"(&mut result, i, byte);
        }
        for let mut i: i32 = 0; (i < len_b); i = (i + 1) {
            let byte: i32 = "String::get"(&b, i);
            "String::set"(&mut result, (len_a + i), byte);
        }
        break __inline_string_concat_4: result;
    });
    core::cli::println(move "\n=== Testing nested ===");
    if ((true && false) || side_effect(move "nested: should print", true)) {
        core::cli::println(move "nested branch taken (correct)");
    }
    if (false && (true || side_effect(move "nested: should not print", true))) {
        core::cli::println(move "should not reach here");
    } else {
        core::cli::println(move "nested && short-circuited (correct)");
    }
}

fn "Array<i32>^IndexValue::index_value"(self: &Array<i32>, index: i32) -> i32 {
    return core::builtin::array_get::<i32>(self.repr, index);
}

pub fn "Array<i32>::len"(self: &Array<i32>) -> i32 {
    return self.used;
}

