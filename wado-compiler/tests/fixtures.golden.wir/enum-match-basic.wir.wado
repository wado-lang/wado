// Golden file: WIR with -O2 optimization
// Source: wado-compiler/tests/fixtures/enum-match-basic.wado
// Generated by: make update-golden-wir-fixtures

struct Box<i32> {  // Box<T> with T=i32
    mut value: i32,
}

array array<u8> (mut u8);

struct String {
    mut repr: ref array<u8>,
    mut used: i32,
}

array array<u64> (mut u64);

struct Array<u64> {  // Array<T> with T=u64
    mut repr: ref array<u64>,
    mut used: i32,
}

enum Color { Red = 0, Green = 1, Blue = 2 };

type "functype/mem/realloc" = fn(i32, i32, i32, i32) -> i32;

type "functype/wasi/stream-drop-writable" = fn(i32);

type "functype/wasi/stream-new" = fn() -> i64;

type "functype/wasi/stream-write" = fn(i32, i32, i32) -> i32;

type "functype/wasi/subtask-drop" = fn(i32);

type "functype/wasi/task-return" = fn(i32);

type "functype/wasi/waitable-join" = fn(i32, i32);

type "functype/wasi/waitable-set-new" = fn() -> i32;

type "functype/wasi/waitable-set-wait" = fn(i32, i32) -> i32;

type "functype/wasi/wasi:cli/Stdout::write_via_stream" = fn(i32, i32) -> i32;

type "functype/run" = fn();

type "functype/__cm_export__run" = fn();

type "functype/core:cli/write_to_stream" = fn(i32, ref null String, bool);

type "functype/core:cli/println" = fn(ref null String);

type "functype/core:internal/wait_for_subtask" = fn(i32);

type "functype/__initialize_module" = fn();

type "functype/Array<u64>^SequenceLiteralBuilder::build" = fn(ref null Array<u64>) -> ref null Array<u64>;

type "functype/Array<u64>^SequenceLiteralBuilder::push_literal" = fn(ref null Array<u64>, u64);

type "functype/Array<u64>::append" = fn(ref null Array<u64>, u64);

import fn realloc from "mem/realloc";
import fn stream-drop-writable from "wasi/stream-drop-writable";
import fn stream-new from "wasi/stream-new";
import fn stream-write from "wasi/stream-write";
import fn subtask-drop from "wasi/subtask-drop";
import fn task-return from "wasi/task-return";
import fn waitable-join from "wasi/waitable-join";
import fn waitable-set-new from "wasi/waitable-set-new";
import fn waitable-set-wait from "wasi/waitable-set-wait";
import fn wasi:cli/Stdout::write_via_stream from "wasi/wasi:cli/Stdout::write_via_stream";
import memory (1) from "mem/memory";

global mut __modules_initialized: bool = 0;

fn run() with Stdout {
    let __local_0: enum:Color;
    let __local_1: enum:Color;
    let __local_2: enum:Color;
    __inline___initialize_modules_0: block {
        if __modules_initialized {
            break __inline___initialize_modules_0;
        };
        __initialize_module();
        __modules_initialized = 1;
    };
    "core:cli/println"(__inline_color_name_1: block -> ref null String {
        __local_0 = 0;
        let __match_scrut_0: enum:Color;
        __match_scrut_0 = __local_0;
        if __match_scrut_0 == 0 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[0](0, 3)), used: 3 };
        } else if __match_scrut_0 == 1 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[1](0, 5)), used: 5 };
        } else if __match_scrut_0 == 2 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[2](0, 4)), used: 4 };
        } else {
            unreachable;
        };
        break __inline_color_name_1;
    });
    "core:cli/println"(__inline_color_name_2: block -> ref null String {
        __local_1 = 1;
        let __match_scrut_1: enum:Color;
        __match_scrut_1 = __local_1;
        if __match_scrut_1 == 0 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[0](0, 3)), used: 3 };
        } else if __match_scrut_1 == 1 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[1](0, 5)), used: 5 };
        } else if __match_scrut_1 == 2 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[2](0, 4)), used: 4 };
        } else {
            unreachable;
        };
        break __inline_color_name_2;
    });
    "core:cli/println"(__inline_color_name_3: block -> ref null String {
        __local_2 = 2;
        let __match_scrut_2: enum:Color;
        __match_scrut_2 = __local_2;
        if __match_scrut_2 == 0 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[0](0, 3)), used: 3 };
        } else if __match_scrut_2 == 1 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[1](0, 5)), used: 5 };
        } else if __match_scrut_2 == 2 -> ref null String {
            String { repr: ref.as_non_null(array.new_data<u8>[2](0, 4)), used: 4 };
        } else {
            unreachable;
        };
        break __inline_color_name_3;
    });
}

fn __cm_export__run() {
    run();
    "wasi/task-return"(0);
}

fn "core:cli/write_to_stream"(tx, message, add_newline) {  // from core:cli
    let len: i32;
    let msg_bytes: ref null array<u8>;
    let alloc_size: i32;
    let ptr: i32;
    let i: i32;
    let byte: u8;
    let __local_9: ref null String;
    let __local_10: ref null String;
    len = message.used;
    msg_bytes = message.repr;
    alloc_size = if add_newline -> i32 {
        len + 1;
    } else {
        len;
    };
    ptr = "mem/realloc"(0, 0, 1, alloc_size);
    __for_0: block {
        i = 0;
        block {
            l12: loop {
                if (i < len) == 0 {
                    break __for_0;
                };
                block {
                    byte = builtin::array_get_u8(msg_bytes, i);
                    builtin::store_u8(ptr + i, byte);
                };
                i = i + 1;
                continue l12;
            };
        };
    };
    if add_newline {
        builtin::store_u8(ptr + len, 10);
    };
    drop("wasi/stream-write"(tx, ptr, alloc_size));
    "wasi/stream-drop-writable"(tx);
}

fn "core:cli/println"(message) with Stdout {  // from core:cli
    let handles: i64;
    let rx: i32;
    let tx: i32;
    let subtask: i32;
    handles = "wasi/stream-new"();
    rx = builtin::i32_wrap_i64(handles);
    tx = builtin::i32_wrap_i64(handles >> 32_i64);
    subtask = "wasi/wasi:cli/Stdout::write_via_stream"(rx, 2048);
    "core:cli/write_to_stream"(tx, message, 1);
    "core:internal/wait_for_subtask"(subtask);
}

fn "core:internal/wait_for_subtask"(packed) {  // from core:internal
    let handle: i32;
    let ws: i32;
    let ptr: i32;
    handle = packed >> 4;
    if handle != 0 {
        ws = "wasi/waitable-set-new"();
        "wasi/waitable-join"(handle, ws);
        ptr = "mem/realloc"(0, 0, 4, 8);
        drop("wasi/waitable-set-wait"(ws, ptr));
        drop("mem/realloc"(ptr, 8, 4, 0));
        "wasi/subtask-drop"(handle);
    };
}

fn __initialize_module() {
    let __local_0: ref null Array<u64>;
    let __local_1: i32;
    let __local_2: i32;
}

fn Array<u64>^SequenceLiteralBuilder::build(self) {
    return self;
}

fn Array<u64>^SequenceLiteralBuilder::push_literal(self, value) {
    Array<u64>::append(self, value);
}

fn Array<u64>::append(self, value) {
    let used: i32;
    let capacity: i32;
    let new_capacity: i32;
    let new_repr: ref null array<u64>;
    let __local_6: i32;
    let __local_7: i32;
    used = self.used;
    capacity = builtin::array_len(self.repr);
    if builtin::unlikely(used >= capacity) {
        new_capacity = __inline_i32_max_0: block -> i32 {
            __local_6 = capacity * 2;
            break __inline_i32_max_0: select i32(__local_6 > 4, __local_6, 4);
        };
        new_repr = builtin::array_new<u64>(new_capacity);
        builtin::array_copy<u64>(new_repr, 0, self.repr, 0, used);
        self.repr = ref.as_non_null(new_repr);
    };
    builtin::array_set<u64>(self.repr, used, value);
    self.used = used + 1;
}

export fn __cm_export__run as "run"

