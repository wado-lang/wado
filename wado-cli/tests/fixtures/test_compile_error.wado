// A file that does not compile. Used to verify that `wado test` reports
// compile failures on the `compile` axis and exits non-zero, without
// aborting the run for other files.

fn main() {
    let x: i32 = "not an integer";
}
