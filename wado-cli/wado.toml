[package]
name = "wado-cli"
version = "0.1.0"

[test]
# CLI integration test fixtures (`tests/fixtures/`) are intentionally
# broken (e.g. `test_compile_error.wado`, `test_fail.wado`) so that
# `assert_cmd` tests in `tests/cli.rs` can verify wado-cli's failure
# reporting. They have no value as discovered Wado tests.
exclude = ["tests/**"]
